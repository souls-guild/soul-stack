// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             v7.34.1
// source: v1/beacon.proto

package pluginv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	SoulBeacon_Validate_FullMethodName = "/soulstack.plugin.v1.SoulBeacon/Validate"
	SoulBeacon_Check_FullMethodName    = "/soulstack.plugin.v1.SoulBeacon/Check"
)

// SoulBeaconClient is the client API for SoulBeacon service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// SoulBeacon — service-контракт плагинов `kind: soul_beacon` (ADR-020 amendment
// V5-2, 2026-05-26, четвёртый kind после soul_module / cloud_driver / ssh_provider).
//
// Host — `soul`-бинарь (соответствие semantic «beacon observes host»). Бинари
// плагинов — soul-beacon-<name>. Адресация Vigil — `<namespace>.<name>` в поле
// `VigilDef.check`; диспетчер Soul-side различает встроенные `core.beacon.*` от
// plugin-beacon по namespace.
//
// Lifecycle (one-shot per Spawn по ADR-020(d)): Soul-side scheduler вызывает
// плагин per-tick (interval из VigilDef.interval) — Spawn → Check → Close. Поле
// `state_cookie` в Reply/Request позволяет плагину сохранять inotify-fd /
// in-memory state между ticks без long-lived процесса: host передаёт cookie
// предыдущего тика обратно при следующем Spawn (NULL на первом).
//
// stream-формы (`Start`) сознательно НЕ вводятся — ADR-030(d) one-shot
// lifecycle и edge-triggered scheduler уже на Soul-side; stream дал бы
// дублирующую ответственность планирования.
type SoulBeaconClient interface {
	// Validate — runtime-проверки params Vigil (то, что не выражается JSON Schema
	// manifest-а: достижимость пути, валидность regex и т.п.). Вызывается host-ом
	// ОПЦИОНАЛЬНО при первом старте Vigil в snapshot; ошибки/Ok=false → Vigil не
	// запускается (scheduler логирует, baseline не устанавливается).
	//
	// Имя ValidateVigilRequest/Reply отличается от ValidateRequest/Reply в
	// soulmodule.proto: в пределах одного proto-пакета имена message-ей должны
	// быть уникальны (cross-import между soulmodule.proto и beacon.proto не
	// нужен — это разные kind-ы плагинов).
	Validate(ctx context.Context, in *ValidateVigilRequest, opts ...grpc.CallOption) (*ValidateVigilReply, error)
	// Check — invoked per-tick для одной Vigil-row. Возвращает state (opaque
	// строка для edge-triggered сравнения), payload (Struct для
	// PortentEvent.payload.custom) и state_cookie (passback host для следующего
	// тика).
	Check(ctx context.Context, in *CheckRequest, opts ...grpc.CallOption) (*CheckReply, error)
}

type soulBeaconClient struct {
	cc grpc.ClientConnInterface
}

func NewSoulBeaconClient(cc grpc.ClientConnInterface) SoulBeaconClient {
	return &soulBeaconClient{cc}
}

func (c *soulBeaconClient) Validate(ctx context.Context, in *ValidateVigilRequest, opts ...grpc.CallOption) (*ValidateVigilReply, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ValidateVigilReply)
	err := c.cc.Invoke(ctx, SoulBeacon_Validate_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *soulBeaconClient) Check(ctx context.Context, in *CheckRequest, opts ...grpc.CallOption) (*CheckReply, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(CheckReply)
	err := c.cc.Invoke(ctx, SoulBeacon_Check_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// SoulBeaconServer is the server API for SoulBeacon service.
// All implementations must embed UnimplementedSoulBeaconServer
// for forward compatibility.
//
// SoulBeacon — service-контракт плагинов `kind: soul_beacon` (ADR-020 amendment
// V5-2, 2026-05-26, четвёртый kind после soul_module / cloud_driver / ssh_provider).
//
// Host — `soul`-бинарь (соответствие semantic «beacon observes host»). Бинари
// плагинов — soul-beacon-<name>. Адресация Vigil — `<namespace>.<name>` в поле
// `VigilDef.check`; диспетчер Soul-side различает встроенные `core.beacon.*` от
// plugin-beacon по namespace.
//
// Lifecycle (one-shot per Spawn по ADR-020(d)): Soul-side scheduler вызывает
// плагин per-tick (interval из VigilDef.interval) — Spawn → Check → Close. Поле
// `state_cookie` в Reply/Request позволяет плагину сохранять inotify-fd /
// in-memory state между ticks без long-lived процесса: host передаёт cookie
// предыдущего тика обратно при следующем Spawn (NULL на первом).
//
// stream-формы (`Start`) сознательно НЕ вводятся — ADR-030(d) one-shot
// lifecycle и edge-triggered scheduler уже на Soul-side; stream дал бы
// дублирующую ответственность планирования.
type SoulBeaconServer interface {
	// Validate — runtime-проверки params Vigil (то, что не выражается JSON Schema
	// manifest-а: достижимость пути, валидность regex и т.п.). Вызывается host-ом
	// ОПЦИОНАЛЬНО при первом старте Vigil в snapshot; ошибки/Ok=false → Vigil не
	// запускается (scheduler логирует, baseline не устанавливается).
	//
	// Имя ValidateVigilRequest/Reply отличается от ValidateRequest/Reply в
	// soulmodule.proto: в пределах одного proto-пакета имена message-ей должны
	// быть уникальны (cross-import между soulmodule.proto и beacon.proto не
	// нужен — это разные kind-ы плагинов).
	Validate(context.Context, *ValidateVigilRequest) (*ValidateVigilReply, error)
	// Check — invoked per-tick для одной Vigil-row. Возвращает state (opaque
	// строка для edge-triggered сравнения), payload (Struct для
	// PortentEvent.payload.custom) и state_cookie (passback host для следующего
	// тика).
	Check(context.Context, *CheckRequest) (*CheckReply, error)
	mustEmbedUnimplementedSoulBeaconServer()
}

// UnimplementedSoulBeaconServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedSoulBeaconServer struct{}

func (UnimplementedSoulBeaconServer) Validate(context.Context, *ValidateVigilRequest) (*ValidateVigilReply, error) {
	return nil, status.Error(codes.Unimplemented, "method Validate not implemented")
}
func (UnimplementedSoulBeaconServer) Check(context.Context, *CheckRequest) (*CheckReply, error) {
	return nil, status.Error(codes.Unimplemented, "method Check not implemented")
}
func (UnimplementedSoulBeaconServer) mustEmbedUnimplementedSoulBeaconServer() {}
func (UnimplementedSoulBeaconServer) testEmbeddedByValue()                    {}

// UnsafeSoulBeaconServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to SoulBeaconServer will
// result in compilation errors.
type UnsafeSoulBeaconServer interface {
	mustEmbedUnimplementedSoulBeaconServer()
}

func RegisterSoulBeaconServer(s grpc.ServiceRegistrar, srv SoulBeaconServer) {
	// If the following call panics, it indicates UnimplementedSoulBeaconServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&SoulBeacon_ServiceDesc, srv)
}

func _SoulBeacon_Validate_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ValidateVigilRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(SoulBeaconServer).Validate(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: SoulBeacon_Validate_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(SoulBeaconServer).Validate(ctx, req.(*ValidateVigilRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _SoulBeacon_Check_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CheckRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(SoulBeaconServer).Check(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: SoulBeacon_Check_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(SoulBeaconServer).Check(ctx, req.(*CheckRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// SoulBeacon_ServiceDesc is the grpc.ServiceDesc for SoulBeacon service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var SoulBeacon_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "soulstack.plugin.v1.SoulBeacon",
	HandlerType: (*SoulBeaconServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Validate",
			Handler:    _SoulBeacon_Validate_Handler,
		},
		{
			MethodName: "Check",
			Handler:    _SoulBeacon_Check_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "v1/beacon.proto",
}
