kind: soul_module
protocol_version: 1
namespace: core
name: noop

# No-op не выполняет действий — capabilities не требуются.
required_capabilities: []

spec:
  states:
    run:
      description: No-op. Ничего не делает, всегда успех без change (changed=false). barrier-якорь / placeholder; ADR-015.
      input: {}
