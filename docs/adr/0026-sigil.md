# ADR-026. Sigil — plugin integrity (Keeper-signed digest index)

> **⚠ Re-keyed 2026-08-06 (NIM-377 / NIM-438, landed) — see the [amendment](#amendment-2026-08-06-nim-377-the-registry-keys-on-the-artifact-source-the-signature-is-not-a-control-on-declarations).** The registry keys on `(source, ref)`, the cache slot is `<alias>/<commit_sha>/`, the signed block is `soul-stack/sigil/**v2**`, and `manifest_raw`/`manifest` collapsed to one `schema` column (migration **115**, which empties the table because no v1 signature verifies against a v2 block; this banner and the amendment below both said 113 until the [2026-09-04 amendment](#amendment-2026-09-04-nim-794-the-grant-carries-a-list-of-artifacts-and-the-bytes-stop-travelling-through-the-keeper) corrected it — 113 is `113_subject_four_dimensions`, an unrelated migration). The banner below describes the shape before that.
>
> **Implementation almost complete.** Keeper-side: **git resolver for the plugin catalog** (A1-S1, `keeper/internal/plugingit` — go-git F-fetch, R-nested cache `<ns>-<name>/<commit_sha>/` + `current`, see (g)), signing + registry (S3, `keeper/internal/sigil`, `shared/pluginhost.BuildSigilBlock`), operator-facing allow/revoke/list (S4a REST `POST/GET/DELETE /v1/plugins/sigils*` + S4b MCP `keeper.plugin.*`, permissions `plugin.allow|revoke|list`, audit `plugin.allowed|revoked`). **Host-side verify-against-Sigil — LIVE** (S6): the TOFU first-load branch is replaced with verify by Sigil + a multi-anchor set (`shared/pluginhost`); the TOFU check before every `exec` remains as defense-in-depth. **Multi-anchor rotation of signing keys — LIVE (R3, ADR-026(h))**: registry `sigil_signing_keys` (migration 037, `keeper/internal/sigil/keys.go`), multi-anchor Signer (`LoadSigner`), broadcast `SigilTrustAnchors` + Redis channel `sigil:anchors-changed` (cluster reload), operator-facing rotation (R3-S7, REST `POST/GET /v1/sigil/keys`, `POST /v1/sigil/keys/{key_id}/primary`, `DELETE /v1/sigil/keys/{key_id}` + MCP `keeper.sigil.key.*`, permissions `sigil.key-introduce|retire|list|set-primary`, audit `sigil.key-introduced|retired|primary-set`). The bootstrap reply serves the anchor set **from a live source** (R3-S7), not a start-time snapshot — a new Soul after rotation receives the current set. Not done: the `commit_sha` column in `plugin_sigils` (A1-S3 — an audit marker, OUTSIDE the signature). Full normative Integrity-model spec — [`docs/keeper/plugins.md → Integrity-model`](../keeper/plugins.md#integrity-model).

- **Context.** Plugin integrity (`soul-mod-*` / `soul-cloud-*` / `soul-ssh-*`) currently rests on **TOFU** (trust-on-first-use, [`docs/keeper/plugins.md → Integrity-model`](../keeper/plugins.md#integrity-model)): on the first load of a binary into the host cache, the host **itself** computes the SHA-256 and trusts it "as is", recording it in a sidecar `.sha256`; before every subsequent `exec` the digest is checked. This closes binary substitution **after** deployment into the shared cache, but **does not** close first-load and first-fetch: if an attacker substituted the binary in the artifact source (or at the moment the Keeper checks out the git ref) **before** its first appearance on the host — a malicious plugin passes the integrity gate and is forked with the service-user's privileges (`keeper` plugins have access to Vault / PG / PKI) → **an RCE vector**. This gap is explicitly recorded as "Known gap — TOFU first-load" in plugins.md and requires a **keeper-signed index**. In parallel: an unsigned `manifest.yaml` next to the binary → the declared `side_effects` / `required_capabilities` ([ADR-020(f)/(g)](0020-plugin-infrastructure.md#adr-020-plugin-infrastructure-manifest-format-handshake-lifecycle)) are **forgeable** (the attacker edits the manifest together with the binary). The name of the entity "plugin allow-list registry + allow signature" was not fixed in [naming-rules.md](../naming-rules.md) — a propose-and-wait is needed.
- **Entity — `Sigil`** (the Keeper's seal of trust for a specific plugin binary: digest + signature of a fragment of the allow-list index). Added to [naming-rules.md](../naming-rules.md). Passed propose-and-wait; trust model — **Variant A (Keeper-signed digest index)**.
- **Decision.** **Sigil** is introduced — a Keeper-side plugin allow-list with an allow signature. The Keeper is the sole authority over which binaries are allowed to run.
  - **⚠ (a) — the KEY changes with the [2026-08-06 amendment](#amendment-2026-08-06-nim-377-the-registry-keys-on-the-artifact-source-the-signature-is-not-a-control-on-declarations) (NIM-377).** `(namespace, name, ref)` is no longer available as a key: the artifact carries **no self-name** ([ADR-020(p)](0020-plugin-infrastructure.md#amendment-2026-08-06-nim-377-the-schema-is-generated-from-go-the-artifact-carries-no-name)), so there is nothing in it to key on. The key moves onto the **artifact source** — the `(source, ref)` the operator declared in the catalog. Everything else in (a) is unchanged: the record still enters the registry only when the Archon explicitly allows a specific hash, the authority is still the Keeper, the source of truth is still Postgres. The key change itself is **NIM-438**.

    **(a) Authority and registry.** The Keeper holds an allow-list `(namespace, name, ref) → sha256` in Postgres (new table `plugin_sigils`). A record enters the registry **when the Archon EXPLICITLY allows a plugin** via OpenAPI/MCP — a human confirms "this binary with this hash is allowed". The TOFU semantics "the host decides to trust on its own" is **replaced** with "the authoritative list is maintained by the Keeper". The source of truth for hashes is Postgres, the authority is the Keeper.
  - **⚠ (b) — the signed block gets a THIRD revision with the [2026-09-04 amendment](#amendment-2026-09-04-nim-794-the-grant-carries-a-list-of-artifacts-and-the-bytes-stop-travelling-through-the-keeper) (NIM-794).** The scalar `binary_sha256` becomes a **list of artifacts** `{os, arch, path, sha256}`, and the DST moves to `soul-stack/sigil/v3`. The invariant below is unchanged in force and changes only in arity: the host still refuses to run bytes whose digest was not approved, but "the digest" becomes "the digest of the row this host selected". Also changed there: "the Keeper serves the binary" stops being true — the host fetches the bytes from the artifact source, and the Keeper serves only the seal.

    **(b) Sigil = digest + signature.** When distributing a plugin to a host, the Keeper serves the binary **plus** a fragment of the index signed with its key: `Sigil = sign_keeper(manifest_digest_block)`, where the signed block carries `(namespace, name, ref, binary_sha256, manifest)`. Before seal / `exec`, the Soul (or the Keeper host for its own plugins) checks: (1) the SHA-256 of the actual binary == `binary_sha256` in the Sigil; (2) the Sigil signature is valid under the Keeper's public key. Any mismatch → rejection, the binary does not gain control (as `ErrPluginDigestMismatch` today).
  - **⚠ (c) is CORRECTED by the [2026-08-06 amendment](#amendment-2026-08-06-nim-377-the-registry-keys-on-the-artifact-source-the-signature-is-not-a-control-on-declarations) (NIM-377).** The mechanism below is real and stays — the schema document is under the signature and cannot be substituted without breaking it. The **consequence claimed for it is hollow**: an unforgeable declaration that nothing enforces changes nothing about what the plugin can do. `side_effects` and `required_capabilities` are disclosure to the operator before approval, not controls ([ADR-020(r)](0020-plugin-infrastructure.md#amendment-2026-08-06-nim-377-the-schema-is-generated-from-go-the-artifact-carries-no-name)); making them non-forgeable makes the **disclosure** trustworthy, which is worth having and is all it is. `protocol_version` is the one signed field with a consumer that acts on it.

    **(c) Manifest coverage (idea C).** The Sigil signature covers the `manifest` with the `binary_sha256` field **attached**. Consequence: the declared `side_effects` / `required_capabilities` / `protocol_version` stop being forgeable — the manifest cannot be substituted without breaking the signature. Currently the host trusts an unsigned `manifest.yaml`; with Sigil the manifest becomes part of the signed block.
  - **(d) Root of trust.** The Keeper's signing key lives in **Vault KV** — by analogy with `secret/keeper/jwt-signing-key` ([ADR-014](0014-operator-identity.md#adr-014-operator-identity-model-archon)). The **public** part arrives at the Soul in **bootstrap** together with the CA-chain (through the same `BootstrapReply` channel as the mTLS CA, [ADR-012(f)](0012-keeper-soul-grpc.md#adr-012-keepersoul-grpc-contract-one-eventstream-with-oneof-keeper-side-render-forward-compat-only-add)) — the Soul receives the Sigil signature trust-anchor at exactly the same time as the mTLS trust-anchor.
  - **(e) Transport — only-add.** Distributing a Sigil to the Soul — only-add proto messages in the `oneof payload` of the existing `EventStream` ([ADR-012(c)](0012-keeper-soul-grpc.md#adr-012-keepersoul-grpc-contract-one-eventstream-with-oneof-keeper-side-render-forward-compat-only-add) forward-compat only-add; [ADR-002](0002-transport-grpc-ha.md#adr-002-transport-keeper--souls--grpc-bidirectional-stream-over-mtls-ha-keeper-cluster) "one stream" preserved), no new RPC. The `binary_sha256` field in the `proto/plugin` manifest is a new **optional** field (only-add, not breaking). Names fixed at the S2b implementation: `PluginSigil` (broadcast of one seal), `SigilSnapshot` (full active set, ReplaceAll) and `SigilTrustAnchors` (full anchor set, ReplaceAll) in `FromKeeper.oneof`; the normative semantics of combining them — in (h).
  - **(f) Push / pull modes.** Sigil works in both. **Push** (the Keeper transfers plugins FROM the Keeper to the Soul over mTLS — the Keeper is already a trust-anchor): the Sigil rides along with the binary. **Pull** (Soul daemon): the Soul verifies the Sigil against the Keeper's public key from bootstrap. In both cases verification is before seal / exec.
  - **⚠ (g) — two mechanical details change with the [2026-08-06 amendment](#amendment-2026-08-06-nim-377-the-registry-keys-on-the-artifact-source-the-signature-is-not-a-control-on-declarations) (NIM-377).** The cache slot `<cacheRoot>/<ns>-<name>/<commit_sha>/` becomes `<cacheRoot>/<alias>/<commit_sha>/` — the first component is the **registration alias** the operator chose, not a name read out of the artifact. And `dist/<binary-name>` becomes **the single executable in `dist/`**: with no self-name there is no filename to compute, so the resolver takes the one executable it finds and fails the entry if there is not exactly one. The R-nested shape, `current` symlink, single-active-per-slot, the F-fetch boundary, the go-git hardening and every size limit are unchanged.

    **(g) `ref` semantics — git-verified (Variant A, F-fetch).** The `ref` of the catalog `keeper.yml::plugins.{cloud_drivers,ssh_providers}` is now **git-verified**: the Keeper itself resolves `source`+`ref` into a local cache slot via **go-git** (pure-Go, no fork of the system `git`). The resolution chain is shallow `clone`→`fetch`→`ResolveRevision(<ref>^{commit})` (resolving the ref into a 40-hex `commit_sha`)→detached-HEAD `checkout`; from the checkout the **ALREADY built** binary `dist/<binary-name>` + `manifest.yaml` is extracted (**F-fetch — NO compilation on the Keeper**, the binary comes ready from the plugin repository). The "verified" boundary = "the Keeper checked out exactly this `ref` and fixed the result (`commit_sha` + `binary_sha256`)", **NOT** bit-reproducibility of the build.
    - **Cache layout — R-nested.** `<cacheRoot>/<ns>-<name>/<commit_sha>/` (an immutable per-commit slot: `manifest.yaml` + binary) + symlink `<cacheRoot>/<ns>-<name>/current → <commit_sha>` (an atomically repointable pointer to the active slot). **Single-active-per-pair** on the Keeper side (as on the Soul side — single-active): `current` points to exactly one `commit_sha`, but **several `commit_sha` slots under one `(namespace, name)` coexist** (the earlier claim "single-slot, coexistence of two `ref`s is impossible" is withdrawn). `plugin.allow` reads the binary+manifest of the ACTIVE slot via `current` (`pluginhost.ReadSlot`), `ref` still does not participate in the slot lookup.
    - ⚠ **The DST named in the bullet below is TWO revisions stale.** It became `soul-stack/sigil/v2` with the [2026-08-06 amendment](#amendment-2026-08-06-nim-377-the-registry-keys-on-the-artifact-source-the-signature-is-not-a-control-on-declarations) (the block re-keyed onto `(source, ref)`) and becomes `soul-stack/sigil/v3` with the [2026-09-04 amendment](#amendment-2026-09-04-nim-794-the-grant-carries-a-list-of-artifacts-and-the-bytes-stop-travelling-through-the-keeper) (the scalar `binary_sha256` becomes an artifact list). What the bullet says about `commit_sha` holds in every revision: it stays outside the signed block, and it carries provenance rather than trust. Original text follows.
    - **`commit_sha` — a provenance audit marker, OUTSIDE the signature.** The DST remains `soul-stack/sigil/v1`, the Sigil signature is over the block `(namespace, name, ref, binary_sha256, manifest_sha256)` (see the block format in [plugins.md](../keeper/plugins.md#signed-block-format-normative-s3)); `commit_sha` is **not included** in the signed block — it will be added as a column in `plugin_sigils` as an audit marker ("which commit produced this binary") at S3 (marked "introduced at impl"). **The integrity authority is `sha256` + the Keeper's signature** (invariant (b) is not weakened); `commit_sha`/`ref` carry provenance and readability, not trust.
    - **Git stack — go-git, hardening.** hooks are **not executed** by-design (go-git runs them neither on clone nor on checkout); submodules are **not recursed** (`RecurseSubmodules` is not passed); the `ext::` transport (executing an external command as a "transport") **does not exist at all** in go-git; `file://` is **locked by a scheme allow-list** (`validateGitScheme`: prod allow-list `https://`/`ssh://`/scp-form; `file://` — only under the env flag `SOUL_STACK_ALLOW_FILE_REPOS=1` for dev/test). **No runtime dependency on the `git` binary** on the keeper host. Hardening: shallow (`Depth=1`) + context-timeout (`plugins.fetch_timeout`, default 120s); the working clone (`plugins.work_root`) is **STRICTLY outside `cacheRoot`** — `.git` and the checkout must not land in the cache directory read by Discover/ReadSlot; **size limit by volume** — `plugins.max_clone_size_mb` (default 1024 MiB, the clone working tree: a du-walk of checkout + `.git`, checked after checkout before extracting the artifact) + `plugins.max_artifact_size_mb` (default 256 MiB, the binary `dist/<binary-name>`: `os.Stat` before copy + `io.LimitReader` during copy).
    - **git-egress — HIGH security risk.** An external git call with a config-controlled `source` is an attack surface. `source` is operator-asserted, but the repository is untrusted, and the timeout limits egress only by time; **size limit — IMPLEMENTED** (see above: clone-tree + artifact cap, fail-closed → the slot is not created, there is nothing to allow for the plugin; sentinels `ErrCloneTooLarge`/`ErrArtifactTooLarge`). The remainder of the security pass before prod (`noexec` on the slot, sandboxing git operations) — mandatory, deferred (see Consequences).
    - Per-entry resolution is **fail-closed**: a broken catalog entry (an unresolvable ref / no manifest / no `dist/<binary-name>` / an unreachable remote) turns into a per-entry warning, the Keeper does not crash; sentinels — `ErrRefNotResolved` / `ErrManifestNotFound` / `ErrArtifactNotFound` / `ErrSourceUnavailable`.
  - **(h) Combining single-vs-snapshot and multi-anchor — replace semantics (Variant A).** The transport distributes two kinds of messages; the normative semantics of combining them is as follows:
    - **`SigilSnapshot{repeated PluginSigil}` — the SOLE source of truth** of the active allow set on the Soul side. Applied as **ReplaceAll**: the Soul **replaces its entire** local set of Sigils with this list, does **not** do an upsert. An allow entry absent from the snapshot the Soul **forgets** — this is how revoke/retire works: after an Archon revoke the Keeper sends a new snapshot without the revoked entry, and access closes **near-instant**, without restarting the Soul. An empty `sigils[]` = no plugin is allowed.
    - **A single `FromKeeper.plugin_sigil` (field 5) — the role of a broadcast notification** about a new allow, **NOT a set mutation**. On it the Soul does **NOT upsert** into the authoritative set; the authority is **only the snapshot**. The single message serves as a signal "a new Sigil has appeared" (for example, so the Soul initiates applying a fresh snapshot), but by itself does not change the active set.
    - **`SigilTrustAnchors{repeated pubkey_pem}` — ReplaceAll of the anchor set** of the signature. Runtime delivery of the set **fully replaces** the bootstrap anchors (this is a **replace, not a merge**): after receiving `SigilTrustAnchors` the Soul verifies signatures with **exactly this** set; an anchor absent from the set the Soul forgets (retire of the old key). Multi-anchor allows seamless rotation of the signing key (any anchor from the set verifies the signature).
    - **`BootstrapReply`: priority set > single, LIVE source (R3-S7).** When `sigil_pubkey_pem_set` (field 6) is non-empty, the single `sigil_pubkey_pem` (field 5) is **ignored** (the set is the authority). **Both empty = Sigil disabled** on this host (plugin verify is not performed). The bootstrap handler reads the set **on every reply from a live source** (`TrustAnchorSource`, the same atomic holder that the `sigil:anchors-changed` watcher updates), and **not from a start-time snapshot** — otherwise a new Soul onboarding between a rotation and its first connect would receive a stale set (dangerous on Retire: it would trust the retired key or reject the fresh primary).
    - **Retire invariant (normative, R3-S7).** Removing a key from the set (`Retire`) is safe **only** when two conditions hold: (1) the new set has **propagated across the cluster** — guaranteed by `PublishAnchorsChanged` (Redis channel `sigil:anchors-changed`) → `reloadAnchors` on each node (re-build Signer + re-broadcast `SigilTrustAnchors` + holder update), and **on loss of the pub/sub signal** — a TTL fallback tick (`sigil_anchors_reload_interval`, see (h)) self-heals the lagging node within the interval; propagation is visible to the operator via `keeper_sigil_anchors_last_delivered` **before** Retire; (2) the bootstrap source is **live** — the bootstrap reply serves the current set (the previous item). Both conditions now hold in the implementation. Additionally `Retire` is forbidden for the **primary** directly (first `SetPrimary` to another active key) and for the **last active** (the set must not empty out — symmetric with RBAC self-lockout); the invariants are transactional under `FOR UPDATE` (`keeper/internal/sigil/keys.go`).
    - **Why so.** The authority over integrity is the Keeper's; the Soul must not accumulate a local allow history diverging from the keeper state. ReplaceAll makes revoke and rotation atomic and observable (one snapshot = the full picture), eliminates the desync "the Soul kept a revoked Sigil" without an explicit forget command. The single broadcast as "notification, not mutation" removes the risk that loss/duplication of a single message distorts the set: the set is always restored from the next snapshot.
- **Normative security invariant.** A plugin **does not gain control** until a double check has passed (binary digest == Sigil + a valid Keeper signature). First-load stops being "trust as is" — it becomes "trust only if the Keeper explicitly allowed this hash and signed it". **This is the whole of the invariant, and it is the one real control in the plugin infrastructure.** The schema document (and thus `side_effects` / `capabilities`) is under the signature and not forgeable — but see the correction to (c): non-forgeable is not enforced, and the invariant above does not depend on any declaration being honest. It gates on the digest alone.
- **Reconciliation.** [ADR-020(f)/(g)](0020-plugin-infrastructure.md#adr-020-plugin-infrastructure-manifest-format-handshake-lifecycle) (`required_capabilities` / `side_effects`) — the declarations become **trustworthy as disclosure** thanks to (c); they were never enforced, and (c) does not make them so ([ADR-020(r)](0020-plugin-infrastructure.md#amendment-2026-08-06-nim-377-the-schema-is-generated-from-go-the-artifact-carries-no-name), 2026-08-06 — the earlier wording here, "is strengthened: the declarations become trusted", overstated what a signature over an unenforced field buys). [ADR-012(f)](0012-keeper-soul-grpc.md#adr-012-keepersoul-grpc-contract-one-eventstream-with-oneof-keeper-side-render-forward-compat-only-add) (bootstrap carries the CA-chain) — is extended with the Sigil public key through the same channel. [ADR-014](0014-operator-identity.md#adr-014-operator-identity-model-archon) (Vault KV `secret/keeper/jwt-signing-key`) — sets the pattern for the Sigil key in Vault. [ADR-016](0016-parity-license.md) (community plugins) — places **no burden on plugin authors** (their keys / signatures are not needed); the allow is done by the operator through the Keeper.
- **Consequences.**
  - **Distribution proto contract (introduced at S2b, only-add in the `EventStream` oneof):** `PluginSigil` (one seal-broadcast, `FromKeeper` field 5), `SigilSnapshot{repeated PluginSigil}` (full active set, ReplaceAll, field 7), `SigilTrustAnchors{repeated pubkey_pem}` (full anchor set, ReplaceAll, field 8) + a **new optional field `binary_sha256`** in the `proto/plugin` manifest (only-add). `BootstrapReply` carries the single `sigil_pubkey_pem` (field 5) and the multi-anchor `sigil_pubkey_pem_set` (field 6, priority set > single; see (h)).
  - **New PG table `plugin_sigils`** (`(namespace, name, ref)` → `sha256` + allow audit fields; ⚠ **re-keyed onto `(source, ref)` + a separate active-unique `alias` by migration 115**, see the amendment; this line said 113 until the 2026-09-04 amendment) — the allow-list registry. The exact schema is an implementation task. **The `commit_sha` column** (git-verified binary provenance, (g)) — **introduced at impl S3**, OUTSIDE the signed block (an audit marker, not trust).
  - **Git resolver for the plugin catalog — `keeper/internal/plugingit`** (A1-S1, implemented). At startup the Keeper resolves `keeper.yml::plugins.{cloud_drivers,ssh_providers}` via go-git into the R-nested cache `<cacheRoot>/<alias>/<commit_sha>/` + symlink `current` ((g); `<ns>-<name>` before NIM-377). The artifact convention is **the single executable in `dist/`** (F-fetch; `dist/<binary-name>` before NIM-377). Resolution sentinels: `ErrRefNotResolved` / `ErrManifestNotFound` / `ErrArtifactNotFound` / `ErrSourceUnavailable` / `ErrCloneTooLarge` / `ErrArtifactTooLarge` (per-entry warnings, fail-closed). New config fields — `plugins.work_root` (the root of working clones, STRICTLY outside `cache_root`), `plugins.fetch_timeout` (default 120s), `plugins.max_artifact_size_mb` (default 256 MiB) and `plugins.max_clone_size_mb` (default 1024 MiB); a scheme allow-list on `source` (`validateGitScheme`).
  - **git-egress security pass** (git-egress is a HIGH risk ((g))): **size limit — implemented** (clone-tree + artifact byte-cap, fail-closed, security-audit MEDIUM-1 closed); the remainder (`noexec` on the slot / sandboxing git operations) — mandatory before prod, deferred.
  - **GC of old `commit_sha` slots** (several slots per pair after `ref`/commit rotations) — deferred; a candidate for a Reaper rule, the name — a separate propose-and-wait at impl.
  - **MCP / OpenAPI allow endpoint** — the Archon allows `(namespace, name, ref, sha256)` into the registry; new RBAC permissions (`plugin.allow` / `plugin.revoke` / `plugin.list`) and audit events (`plugin.allowed` / `plugin.revoked` / `plugin.verify_failed`). The CRUD surface starts as a stub catalog (entries in operator-api.md / mcp-tools.md / rbac.md as actually implemented).
  - **Replacement of the TOFU first-load branch** in `shared/pluginhost` with verify-against-Sigil. The TOFU check before every `exec` remains as defense-in-depth for the shared cache; only first-load changes (instead of "trust as is" — "verify Sigil"). Distributing the `shared/pluginhost` change — a separate slice after the ADR review.
  - **The Keeper's signing key in Vault KV** (a new path, following the `secret/keeper/jwt-signing-key` pattern); **the public key in bootstrap** (`BootstrapReply`, next to the CA-chain) — the single `sigil_pubkey_pem` plus the multi-anchor `sigil_pubkey_pem_set` for seamless rotation (R3, replace semantics; runtime delivery of `SigilTrustAnchors` fully replaces the bootstrap anchors, see (h)).
  - **Multi-anchor registry of signing keys (R3, implemented).** New PG table **`sigil_signing_keys`** (migration 037): `key_id` (SHA-256 SPKI, hex) → `pubkey_pem` (SPKI) + `vault_ref` (the private key in Vault) + `is_primary` / `status` (active|retired) + an audit FK to `operators`. **The private key is NEVER in Postgres** — only the public part + a reference to Vault. Invariants: exactly one primary among active (partial unique index `sigil_signing_keys_one_primary`), ≥1 active on Retire. Multi-anchor Signer (`keeper/internal/sigil`): the primary signs new Sigils, the anchor set = the public keys of all active. An empty registry → fallback to the single `sigil.signing_key_ref` from cfg (the old path does not break).
  - **Cluster-wide rotation — Redis channel `sigil:anchors-changed` (R3-S6) + TTL fallback.** After a key-registry mutation (introduce / set-primary / retire) the mutating node does `PublishAnchorsChanged` to `sigil:anchors-changed`; **every** node (including the mutating one) on subscription does `reloadAnchors`: re-build Signer (new primary + anchors) → `SetSigner` into the sigil-service → update of the keeper host's verify set → update of the connect-time broadcast holder → re-broadcast `SigilTrustAnchors` to connected Souls. A separate channel from `sigil:invalidate` (that one is about the `plugin_sigils` allows, this one — about the keys signing them). Pub/sub is best-effort, but **on top of it — a TTL fallback tick** (`sigil_anchors_reload_interval`, default 30s): the same `reloadAnchors` periodically by a ticker, so a missed signal self-heals within the interval (the RBAC TTL-poll pattern [ADR-028(d)](0028-rbac-storage.md#adr-028-rbac-storage--postgres) / `Summons` poll-fallback [ADR-027](0027-apply-work-queue.md#adr-027-apply-execution-model--work-queue--claim-acolyte-pool-ward-claim)). The tick is raised **independently of Redis** (in single-instance/dev without Redis — the only path for runtime rotation to arrive without a restart). `reloadAnchors` is idempotent/fail-safe (on error the re-build leaves state untouched), so a double reload (tick + pub/sub) is safe.
  - **Operator-facing rotation API (R3-S7).** REST `POST /v1/sigil/keys` (introduce: the Keeper generates an ed25519 pair, writes the private key to Vault KV `secret/keeper/sigil-keys/<key_id>`, inserts the public part into the registry; the response carries `key_id` + `pubkey_pem`, **the private key is NEVER returned**), `GET /v1/sigil/keys` (list active), `POST /v1/sigil/keys/{key_id}/primary` (set-primary), `DELETE /v1/sigil/keys/{key_id}` (retire). MCP parity: `keeper.sigil.key.introduce|list|set-primary|retire`. Permissions `sigil.key-introduce|retire|list|set-primary` (resource `sigil`, hyphenated action — the grammar `<resource>.<action>`, the 3-segment `sigil.key.*` = an MCP tool, not a permission). Audit `sigil.key-introduced|retired|primary-set` (payload `key_id` + caller AID, **without the private key**). Metrics `keeper_sigil_signing_keys_active` (gauge of the number of active keys), `keeper_sigil_anchors_rebroadcast_total` (counter of anchor-set re-broadcast passes) and `keeper_sigil_anchors_last_delivered` (gauge of the number of Souls the last set actually reached — the operator sees propagation **BEFORE** Retire). A race on setting primary (the one_primary index, 23505) → sentinel `ErrConcurrentPrimary` → 409 (retry).
  - **TTL fallback for `sigil:anchors-changed` — CLOSED (was a KNOWN GAP).** Propagation of the new anchor set across the cluster was best-effort Redis pub/sub (`PublishAnchorsChanged` → `reloadAnchors`) **without a periodic poll tick**: signal loss → the lagging node held the old anchor set until restart (load-at-start), which is dangerous on **Retire** (the node would locally *fail-open* trust the retired key for a Soul onboarding through it). **Closed** by a TTL fallback tick in `reloadAnchors` (`runAnchorsReloadTicker`, interval `sigil_anchors_reload_interval` default 30s, following the `rbacHolder.Run` TTL-poll / `Summons` poll-fallback pattern): a missed signal self-heals within the interval, the tick is independent of Redis. Observability of re-broadcast delivery — metrics `keeper_sigil_anchors_rebroadcast_total` (passes) + `keeper_sigil_anchors_last_delivered` (how many Souls the set actually reached) + a log of the number of active anchors and delivered on each reload — the operator sees propagation of the set **BEFORE** Retire of the old key.
  - **KNOWN GAP "orphan Vault secret on a PG-fail Introduce".** `POST /v1/sigil/keys` (introduce) writes the private key to Vault KV `secret/keeper/sigil-keys/<key_id>` **before** inserting the public part into `sigil_signing_keys`. If the PG insert fails after a successful Vault write — the private key remains an **orphan** in Vault (no corresponding registry row). Reverse cleanup (deleting from Vault on a PG-fail) is **deliberately not done** — the orphan is harmless (the key is nowhere declared primary/active, no signatures are produced with it), and a compensating transaction across two stores (Vault + PG) is fragile by itself. GC of orphan secrets — a **candidate for a Reaper rule**, deferred together with GC of `commit_sha` slots ((g)). The rule name — a separate propose-and-wait at impl.
  - **Known gap "TOFU first-load" — CLOSED** (S6 LIVE): the Integrity-model section in [plugins.md](../keeper/plugins.md) is rewritten for Sigil + multi-anchor; host-side first-load is now verify-against-Sigil, not TOFU.
  - **Names in [naming-rules.md](../naming-rules.md)** — `Sigil` (+ definition), proto messages `PluginSigil` / `SigilSnapshot` / `SigilTrustAnchors`, the `sigil_pubkey_pem_set` field (priority set > single), PG tables `plugin_sigils` and `sigil_signing_keys`, RBAC perms (`plugin.*` + `sigil.key-*`), audit events (`plugin.*` + `sigil.key-*`), Redis channel `sigil:anchors-changed`.
- **Deferred (post-MVP, no breaking changes).** **Variant B (author-signed collections / publishers)** — a trust boundary at the collection level by `(namespace, ref)` (one publisher → one key) — is deferred until module-collections ([module-collections.md, open Q No. 2](../module-collections.md)). Sigil (Variant A) — allowing specific hashes — is the current MVP model; collections/publishers extend it additively, without breaking the `(namespace, name, ref) → sha256` registry. **git-egress security pass** — the clone/artifact size limit is **implemented** ((g), MEDIUM-1 closed); the remainder (`noexec` on the slot / sandboxing git operations) is mandatory before prod, git-egress is a HIGH risk. **GC of old `commit_sha` slots** — a candidate for a Reaper rule, the name — a separate propose-and-wait. **The `commit_sha` column in `plugin_sigils`** — introduced at S3 (a provenance audit marker, OUTSIDE the signature).
- **Trade-off.** The allow is concentrated on the Keeper (single authority) — but the Keeper is **already** the root of mTLS / Destiny render / Vault access, almost no new trust surface is added. **Zero burden on plugin authors** (their keys / signatures are not needed) — the allow is entirely on the operator through the Keeper; the price — the operator must explicitly allow each `(namespace, name, ref, sha256)` (a human step per new hash), but this is precisely deliberate supply-chain control. The alternative — author-signed (Variant B) — removes the human step, but requires key management by community-plugin authors; deferred.

## Amendment (2026-08-06, NIM-377): the registry keys on the artifact source, the signature is not a control on declarations

Corrects (a), (c) and (g). Decisions settled with the user 2026-08-06; the counterpart on the plugin-infrastructure side is [ADR-020's amendment of the same date](0020-plugin-infrastructure.md#amendment-2026-08-06-nim-377-the-schema-is-generated-from-go-the-artifact-carries-no-name).

### The key moves onto the artifact source

[ADR-020(p)](0020-plugin-infrastructure.md#amendment-2026-08-06-nim-377-the-schema-is-generated-from-go-the-artifact-carries-no-name) removes `namespace:` and `name:` from the artifact: it carries no self-identity at all. `(namespace, name, ref)` therefore has nothing left to read, and **there is no alternative** to keying on what the operator declared — the **artifact source**, `(source, ref)` from the catalog entry.

This is a better key on its own terms, and it is worth saying why, because the old key looked like provenance and was not. `(namespace, name)` came out of the artifact, so two publishers shipping a plugin that called itself `community.redis` collided in the registry, and which one an allow entry referred to depended on which had been resolved into the slot. The source is the thing the operator actually vouched for when they wrote the catalog entry — it cannot be claimed by a second repository.

Registration alias and registry key are **different axes and must not be conflated**: the alias names the address (`redis.acl.present`) and the host slot; the source keys the trust record. One artifact registered under two aliases is one allow decision, not two.

The key change **landed as NIM-438** (migration **115**, `plugin_sigils_source_identity` — this line said 113 until the [2026-09-04 amendment](#amendment-2026-09-04-nim-794-the-grant-carries-a-list-of-artifacts-and-the-bytes-stop-travelling-through-the-keeper)). What it settled:

- **Trust key** — partial unique index on `(source, ref)`: at most one live approval per artifact identity, so re-approving the same artifact is a conflict the operator resolves by revoking first, never a silent second grant that revoking the first would leave standing.
- **Registration invariant** — a *separate* partial unique index on `(alias)`. Not a trust key: one alias is one address space and one host slot, so `<alias>.<module>.<state>` names one set of bytes and the runtime lookup has exactly one answer rather than "whichever row sorted first".
- **Columns** — `namespace`, `name` and the `manifest` jsonb projection dropped; `alias` and `source` added `NOT NULL`; `manifest_raw` renamed to **`schema`** and made `NOT NULL`.
- **Route** — revoke is `DELETE /v1/plugins/sigils/{alias}`, one path parameter. Allow takes `{alias, source, ref}`.
- **Proto** — `PluginSigil` gains `alias` (7, **unsigned**), `source` (8, signed), `schema` (9); fields 1/2/6 (`namespace`/`name`/`manifest`) are **reserved, never reused** ([ADR-012](0012-keeper-soul-grpc.md)), so an old sender against a new receiver populates nothing rather than landing a namespace where an alias is expected.
- **Existing rows are deleted.** Every pre-existing grant was signed under the v1 DST over a block keyed on `(namespace, name)`, so no existing signature verifies against the v2 block. Keeping them would have kept cryptographically dead grants looking active in the UI, and `source` has no value derivable from an old row. Approvals are re-issued with `keeper.plugin.allow`.

**The signed block changes with the key.** The block format is normative and versioned by its DST (`soul-stack/sigil/v1`, see [plugins.md → Signed block format](../keeper/plugins.md#signed-block-format-normative-s3)): changing which fields it covers is a **new DST**, `soul-stack/sigil/v2`, and old signatures stop verifying against new code by construction. That is the intended behaviour, not a migration hazard to work around — there is no release yet, and an allow-list whose entries cannot be re-derived from the catalog is not a state worth preserving.

### What the signature is worth (correction to (c))

(c) says the signature makes `side_effects` / `required_capabilities` non-forgeable and calls that a strengthening of [ADR-020(f)/(g)](0020-plugin-infrastructure.md#adr-020-plugin-infrastructure-manifest-format-handshake-lifecycle). The first half is true. The second is not, and the reason is entirely on the other side of the boundary: **nothing enforces those declarations**. There is no sandbox at spawn, `SideEffects` is read only by the grammar validator, and no production code emits `policy_violation` — the full account is in [ADR-020(r)](0020-plugin-infrastructure.md#amendment-2026-08-06-nim-377-the-schema-is-generated-from-go-the-artifact-carries-no-name).

An unforgeable declaration that nothing enforces is not a weaker control. It is **not a control**. What it is instead is worth stating plainly, because it is genuinely useful:

- **The operator's disclosure is trustworthy.** `plugin.allow` is a human act, and the schema under the signature is what the human reads. Without the signature an attacker could pair a hostile binary with a reassuring declaration; with it, what the operator approved is what the artifact said. That closes a real gap — in the **approval**, not in the runtime.
- **`protocol_version` is the one signed field with a consumer that acts on it.** The host cross-checks it and refuses to start on a mismatch.
- **The control is the digest.** The operator approves a specific sha256; the host refuses to exec anything whose digest differs. It does not consult any declaration, so no declaration being false can weaken it.

The security invariant of this ADR stands unchanged, and it never rested on (c).

### Cache slot and artifact convention (correction to (g))

- **Slot path** — `<cacheRoot>/<alias>/<commit_sha>/`, where `<alias>` is the registration alias from the catalog entry. Previously `<ns>-<name>/`, read out of the artifact. Same on the Soul side (`<paths.modules>/<alias>/`, [ADR-065](0065-core-module-installed.md)) and in `keeper/internal/pluginhost/slot.go` / `shared/pluginhost/discovery.go`.
- **Artifact convention** — **the single executable in `dist/`**, replacing `dist/<binary-name>`. There is no name to compute, so the resolver takes the one executable it finds; **zero or more than one is a per-entry failure** (`ErrArtifactNotFound` / a new ambiguity sentinel — the sentinel set is an implementation detail of the resolver slice), fail-closed like every other broken entry. The size cap `plugins.max_artifact_size_mb` applies to whichever file that is.
- **The schema document replaces `manifest.yaml` in the slot** and comes from the artifact's trailer rather than a checked-out file ([ADR-020(o)](0020-plugin-infrastructure.md#amendment-2026-08-06-nim-377-the-schema-is-generated-from-go-the-artifact-carries-no-name)). `ReadSlot` extracts it **without executing the artifact** — the binary is not approved yet at `plugin.allow` time, which is precisely why the schema had to be readable by seeking rather than by running `--schema`. A missing or malformed trailer is a fail-closed per-entry error; there is no fallback to a neighbouring file.
- **The canon column keeps its role and is renamed `schema`** (`bytea`, now `NOT NULL`), holding the canonical-JSON schema document instead of raw YAML. Normalization collapses to identity — the bytes are byte-deterministic by construction — so the S3↔S6 invariant (Keeper hashes and Soul re-hashes the same bytes) holds trivially rather than by careful agreement between two emitters. **The `manifest` jsonb projection is dropped:** it existed so a hand-written YAML manifest could also be queried, and keeping it now would mean two copies of one value that can disagree, where the one free to drift is the copy the signature was *not* placed over.

Everything else in (g) is untouched: R-nested per-commit slots, the atomically repointed `current`, single-active-per-slot with several `commit_sha` slots coexisting, `commit_sha` as an audit marker outside the signature, the F-fetch boundary ("Keeper checked out exactly this ref", not bit-reproducibility), the go-git hardening, the scheme allow-list and both size caps.

### Not settled here

Whether the Soul-side slot also stores the schema document as a **separate file** beside the executable, or reads it from the artifact's own trailer on each load. Both are consistent with everything above; the trailer makes the bytes inseparable from the binary they describe, which is the property `manifest_raw` was introduced to guarantee by other means. Decided in the wave-2 slot slice, not here.

## Amendment 2026-09-04 (NIM-794): the grant carries a list of artifacts, and the bytes stop travelling through the keeper

**Not implemented — this amendment is still design-only, and deliberately so.** Its subject is the **signed block**: replacing the scalar `binary_sha256` with a list, and the DST bump that forces. That is **NIM-795** (keeper), under epic NIM-793, and nothing of it is in the tree — the block is still `BuildSigilBlock(source, ref, binary_sha256, schema_sha256)` and the DST is still v2. Everything above still describes what ships. Decisions settled with the user 2026-09-04.

⚠ **The Soul half of the same epic HAS shipped (NIM-796, `2daf8545`), and it stopped exactly at this ADR's boundary.** The host now pulls an artifact from its source, selects its platform row and reports the transport it used — see [ADR-065's amendment](0065-core-module-installed.md#amendment-2026-09-04-nim-794-the-fetch-step-goes-to-the-source-and-fetchmodule-stays-as-the-egress-free-path), which is now largely a record of shipped behaviour. What it did **not** do is touch the grant: `SigilRecord` gained the read-side fields `BaseURL` and `Artifacts` (`shared/pluginhost/sigil_verify.go:48-79`) and **nothing fills them from the proto**, because sign and verify are one helper on purpose — both ends move together or neither does. The live consequence is recorded in [ADR-065](0065-core-module-installed.md#what-is-half-wired-and-what-nim-795-inherits) and [known-limitations.md](../known-limitations.md): until the block covers the row list, a grant installs only on the platform whose row digest equals `BinarySHA256hex`, and every other row fetches and then **fails closed at verify**.

The counterparts of the same date are [ADR-065's](0065-core-module-installed.md#amendment-2026-09-04-nim-794-the-fetch-step-goes-to-the-source-and-fetchmodule-stays-as-the-egress-free-path) (which of the install step's six steps changes — **shipped**) and [ADR-020's](0020-plugin-infrastructure.md#amendment-2026-09-04-nim-794-the-catalog-entry-gains-a-source-kind-and-an-explicit-artifact-list) (what the catalog entry looks like — **still design-only**, same reason: it describes what an Archon writes and the Keeper resolves).

### The decision, and what was rejected

A plugin arrives on the host **from its source** — a Nexus repository, a GitHub release — rather than through the Keeper. The Keeper still resolves `ref` into concrete bytes and still signs them; what stops is the bytes travelling through it.

The alternative — **the Keeper proxies the traffic** — was rejected. A shared package proxy already exists in any infrastructure this is deployed into, so routing artifact bytes through the Keeper buys no reachability that is not already there, and it costs load on the Keeper for nothing.

The argument is stronger than "one less hop", and the reason is worth stating rather than leaving to be re-derived. Today those bytes travel on the **same mTLS connection to the Keeper that carries run dispatch**: `FetchModule` is a separate HTTP/2 stream ([ADR-065(a)](0065-core-module-installed.md#a-fetch-transport--server-streaming-rpc-fetchmodule), which is why it is not the rejected "bytes over EventStream"), but the fetcher is taken from the live EventStream session (`fetchViaStream`, `soul/internal/coremod/module/source.go:120-129` since NIM-796; it was `installed.go:96`), so it is the same connection, the same Keeper process and the same network path. Stream isolation keeps a megabyte artifact from blocking the message queue; it does not stop the artifact from competing for that connection's bandwidth, and it does not make the Keeper the right thing to be shipping bytes with. A package proxy is built for exactly this and is already present.

### One grant per `(source, ref)`, whose signed block carries an artifact list

The signed block's scalar `binary_sha256` becomes a **list of artifacts**, one entry per platform:

```
{os, arch, path, sha256}
```

`path` is where the artifact sits under the entry's base URL; `sha256` is the digest approved for that platform.

**Nothing simpler works, and it is worth being precise about why.** The tempting shape is "one digest plus `{os}` substituted into the path". It cannot work: the binaries are different builds, so they have different hashes. A single digest verifies on exactly one platform and refuses on every other, which is not a narrower guarantee but a non-functioning one.

**A grant per platform was rejected too**, and there are two reasons rather than one. The soft reason is cost: N approvals per release, and the operator is answering the same question N times about one artifact identity, with no gain in guarantee. The hard reason is that **this ADR's own trust key forbids it.** The key is a partial unique index on `(source, ref)` — "at most one live approval per artifact identity", [above](#the-key-moves-onto-the-artifact-source) — implemented as `plugin_sigils_active_idx` (`keeper/migrations/115_plugin_sigils_source_identity.up.sql:103`). A per-platform grant needs N live rows on one `(source, ref)`, which that index refuses. The artifact-list shape is therefore not merely the preferred shape; it is **the only shape the existing trust key admits**, and changing the key to admit the other one would mean giving up the property the key was introduced for.

### The DST goes to `soul-stack/sigil/v3`, and this is forced

`BuildSigilBlock` (`shared/pluginhost/sigil.go:79`) assembles

```
DST || LP(source) || LP(ref) || LP(binary_sha256) || LP(schema_sha256)
```

and its own doc comment states the constraint (`shared/pluginhost/sigil.go:61-63`): the field order is fixed and cannot change without bumping the DST to v3. This ADR already ruled the general case at the [2026-08-06 amendment](#amendment-2026-08-06-nim-377-the-registry-keys-on-the-artifact-source-the-signature-is-not-a-control-on-declarations) — changing which fields the block covers is a **new DST**. Replacing one length-prefixed digest with a list of them is exactly that change, so the DST becomes `soul-stack/sigil/v3` by the rule already written down, not by a fresh decision.

**Every existing grant is deleted, and that is the correct behaviour rather than a hazard to migrate around.** This is the same consequence, for the same reason, as v1→v2: no signature over a v2 block verifies against a v3 block, so a retained row is a cryptographically dead grant that still looks active in the UI. The v1→v2 reasoning applies unchanged — an allow-list whose entries cannot be re-derived from the catalog is not a state worth preserving, and approvals are re-issued with `keeper.plugin.allow`.

### The source is untrusted by construction, and source-pull weakens nothing

The instinct on reading "the host downloads from Nexus" is that trust moved to Nexus. It did not, and the reason is already in this ADR: (g) above says plainly that `source` is **operator-asserted, but the repository is untrusted**. That was true when the Keeper did the fetching and it is true when the host does. Nothing in the trust model ever rested on where the bytes came over the wire from.

**The gate is the digest.** An artifact gains control only once its sha256 matches an entry in an approved artifact list, checked before the bytes are materialized — so a compromised source earns a **verification refusal, not code execution**. The carrier of that approval today is the Keeper's signature over the block, verified against the trust-anchor set. This is deliberately worded the way [ADR-020(r)](0020-plugin-infrastructure.md#amendment-2026-08-06-nim-377-the-schema-is-generated-from-go-the-artifact-carries-no-name) words it — "the operator approves a specific sha256 and the host refuses to exec anything whose digest differs" — so that the invariant is stated over the digest and stays true if the signature mechanism is later revisited.

Say this out loud in any follow-up rather than assuming it: without it the next reader concludes the ticket traded safety for convenience, and then treats the source as something that has to be trusted.

**Revocation does not change.** It works through `SigilSnapshot` with ReplaceAll ((h) above) — through the **set of grants**, not through the bytes. Who downloaded which bytes when is irrelevant to it: a host with the artifact already on disk loses the grant that lets it run, which is the same mechanism and the same latency as today.

### There is deliberately no path template

The artifact list is explicit — one row per platform, each naming its own `path`. There is no template, and its absence is a decision rather than an omission.

A URL is where executable bytes come from. An expressive template there turns an address into a small program, evaluated on the host, whose output selects what gets executed. The explicit list closes that question **structurally**: there is nothing to evaluate, so there is nothing to get wrong or to steer. It is not closed by trusting the proxy to serve only what it should.

[ADR-020(p)](0020-plugin-infrastructure.md#amendment-2026-08-06-nim-377-the-schema-is-generated-from-go-the-artifact-carries-no-name) reinforces this from the other side: **the artifact carries no subject name**, and nothing in the bytes says which platform they are for. So the list is not merely the safer way to state the mapping — nothing else can state it. The list has to be explicit because the artifact cannot be asked.

**The mechanical half of this decision has SHIPPED (NIM-796), and lives on the host.** "No template" is a statement about the catalog; what makes it enforceable is that the host **joins** `base_url` and the row's `path` and refuses anything that could steer the address — an absolute path, a URL of its own, a `.` or `..` segment, a query or a fragment, checked as written **and** percent-decoded, plus a `base_url` with no host, with credentials, or with a query. The rules and their reasons are in [ADR-065's amendment](0065-core-module-installed.md#the-three-security-decisions-the-source-pull-is-built-on) (c), which is the enforcement side of this paragraph; the two are deliberately one decision recorded in two places rather than two decisions that could drift.

### Authentication to the source is deliberately deferred

v1 assumes an **anonymously readable** repository. This is a deferral with a reason, not an oversight.

Authenticating to the source means a credential on **every managed host**. There is no such secret there today, and putting one there is its own security decision — where it lives, who rotates it, what its compromise reaches — of a size that belongs in its own ticket rather than riding along with a transport change. An internal Nexus that serves reads anonymously covers the case in front of us.

**The deferral is enforced rather than assumed (NIM-796).** A `base_url` carrying credentials is **refused**, not carried: the host sends no `Authorization` header and has none to send, and a `https://user:pass@…` that were merely passed through would put a password into `fetch_url` — that is, into `RunResult` and OTel. So the shipped behaviour is a refusal at the address, not an anonymous request that happens to work.

### `ref` for an artifact-kind entry is a label, not a checkout

(g) above defines "verified" as "the Keeper checked out exactly this `ref` and fixed the result". For an **artifact-kind** entry there is no checkout: nothing is cloned, and there is no `commit_sha` to record. `ref` is a label the operator asserts, sitting in the signed block for the same reason it does today.

The loss is bounded and (g) already says so: `commit_sha`/`ref` **carry provenance and readability, not trust**. So what an artifact-kind entry gives up is a provenance marker, not a control. It is named here rather than omitted, because "the Keeper checked out exactly this ref" reads as a guarantee and for these entries it is not one.

### An OPEN fork: what fills `source` for an artifact-kind entry

**Not decided here, and deliberately not decided in passing.** This is the second open fork of this ticket, beside [ADR-020's (o)](0020-plugin-infrastructure.md#o--an-open-fork-deliberately-not-closed-here) — N stamped schema trailers against one signed `schema_sha256`. Both belong to NIM-795 and both interact with the pending decision on the signature mechanism, so they are best looked at together rather than one being settled under the other's assumptions.

**The fork.** [naming-rules.md](../naming-rules.md) says `base_url` **"replaces `source` for that arm — nothing is cloned, so there is no git remote to name"**, and the catalog example in [ADR-020's amendment of this date](0020-plugin-infrastructure.md#amendment-2026-09-04-nim-794-the-catalog-entry-gains-a-source-kind-and-an-explicit-artifact-list) carries no `source:` key at all. Yet nothing here retires `source`: this amendment keeps grant identity at `(source, ref)` on purpose — that is what makes the artifact-list shape [the only shape the trust key admits](#one-grant-per-source-ref-whose-signed-block-carries-an-artifact-list). So a value still has to reach `source`, and which value is **unstated**.

**`source` is not a cosmetic field, which is why an implementer must not be left to pick.** Three load-bearing uses, each independently sufficient:

- **`NOT NULL` in PG** — `ADD COLUMN source TEXT NOT NULL` (`keeper/migrations/115_plugin_sigils_source_identity.up.sql:83`). There is no writing a grant without one.
- **The first length-prefixed field of the signed block** — `appendLP(block, []byte(source))` (`shared/pluginhost/sigil.go:91`). Whatever goes in is covered by the signature, so a convention changed afterwards invalidates every grant issued under the old one, at the cost of a DST bump without the benefit of one.
- **Half the trust key** — `plugin_sigils_active_idx ON plugin_sigils (source, ref) WHERE revoked_at IS NULL` (`:103-105`). "At most one live approval per artifact identity" is *measured* on this column: two entries that collapse to one `source` cannot both hold a live grant, and two that ought to be one identity but spell `source` differently get two.

**The presumptive answer is `base_url`** — it is the entry's only address, it is stable per artifact identity in the way a git remote is, and it keeps the trust key reading "one live approval per place the bytes come from". It is recorded as presumptive rather than decided because the cases that would settle it have not been checked: two `base_url` values differing only by a trailing slash or by host case, two aliases pointed at one `base_url`, and whether the value is normalized before it is signed — normalization is itself part of the convention, since the signature covers the bytes as given.

**NIM-795 comes back with an answer rather than assuming one.** Filling `source` silently — from `base_url`, from the alias, or leaving it for the `NOT NULL` to refuse at write time — writes a convention into signed bytes and into a unique index, where changing it later costs every grant in the table. That is architecture, and a silent pick is exactly the outcome this amendment and its counterparts otherwise exist to prevent.

### Contract impact — additive where it can be, breaking where it cannot

**Proto, the only-add half.** `PluginSigil` (`proto/keeper/v1/sigil.proto:29-55`) already reserves 1, 2 and 6. The additive form is a new `repeated PluginArtifact artifacts = 10`.

**Proto, the breaking half.** `binary_sha256 = 4` must become `reserved 4; reserved "binary_sha256";` the **moment it stops being authoritative** — never-reuse per [ADR-012](0012-keeper-soul-grpc.md), the same treatment fields 1/2/6 got here and `KIND_CLOUD_DRIVER` got at [ADR-020(u)](0020-plugin-infrastructure.md#amendment-2026-09-01-nim-757-cloud_driver-is-removed-and-side-keeper-is-what-replaces-it) (`:325-326`).

★ **The trap to state before someone proposes it:** keeping field 4 populated as "one of the N", for compatibility, makes an old Soul **silently install whichever row the Keeper happened to put there** — no error, no mismatch, an artifact for the wrong platform materialized under a valid signature. A field that used to mean "the approved digest" and now means "one of several" is worse than a field that is gone, because the reader that has not been updated cannot tell.

**Breaking consumers, named.** Each of these reads a scalar digest and becomes a set membership or a list:

| Site | What changes |
|---|---|
| `shared/pluginhost/integrity.go:130-166` | `verifyRecordAgainstDigest` — the constant-time equality at `:158` becomes membership in the approved set; the block it rebuilds at `:163` becomes the v3 block. |
| `shared/pluginhost/sigil_verify.go:44` | `SigilRecord.BinarySHA256hex` — scalar → list. ⚠ **Half-landed by NIM-796:** the record already carries `BaseURL` and `Artifacts []SigilArtifact` beside it (`:48-79`), populated by nothing; the scalar is untouched and still the only thing verify reads. |
| `soul/internal/pluginhost/sigil_adapter.go:51` | Populates that field from `PluginSigil.GetBinarySha256()`. |
| `keeper/cmd/keeper/daemon.go:1283-1300` | `sigilRecordLister.ListActive` populates it from the store row. |
| `keeper/internal/sigil/lookup.go:34-64` | `LookupModuleBinary` matches a request against one `rec.SHA256` and re-reads one slot. See the [ADR-065 amendment](0065-core-module-installed.md#amendment-2026-09-04-nim-794-the-fetch-step-goes-to-the-source-and-fetchmodule-stays-as-the-egress-free-path) for why this one is load-bearing beyond its own edit. |
| `soul/internal/coremod/module/installed.go:91,98,212` | Idempotency compare, fetch by content address, and the result payload. ⚠ **Renumbered by NIM-796**, which also moved the fetch itself into `source.go` and gave the payload its `fetch_via` / `fetch_url` keys; all three still read the scalar. |
| `plugin_sigils.sha256` | A single `TEXT NOT NULL` column with `CHECK (sha256 ~ '^[0-9a-f]{64}$')` (`keeper/migrations/028_create_plugin_sigils.up.sql:61,69-71`) — one digest per row cannot hold N. |

**Breaking, and reaching a repository this one cannot build.** The Operator API's `PluginSigilView` (`docs/keeper/openapi.yaml:2564-2592`) and `PluginSigilAllowReply` (`:2515-2532`) each carry `sha256` as a **required** scalar with a 64-hex pattern. `soul-stack-web` regenerates `types.gen.ts` from this document, and that regeneration is caught by **neither core `make check` nor core CI** — so the breakage surfaces in the companion repository, after the fact. NIM-795 has to carry it deliberately.

`PluginSigilAllowRequest` (`:2533-2551`) is **unchanged**: it takes `{alias, source, ref}` and no digest. The operator never sends a digest — the Keeper resolves and hashes the bytes itself — so the request shape is indifferent to how many of them there are.

**Plugin authors are unaffected.** The "zero burden on plugin authors" trade-off above holds: no author key, no author signature, and nothing new to publish beyond the per-platform binaries a release already contains.

### A factual correction made while in this file

The re-key of `plugin_sigils` landed as migration **115** (`keeper/migrations/115_plugin_sigils_source_identity.up.sql`), not 113. The banner, the NIM-438 amendment and the Consequences bullet all said 113; `113_subject_four_dimensions` is an unrelated migration. All three are corrected in place, and the correction is recorded here so a reader tracing NIM-438 through the migration ladder is not left looking at the wrong file. [ADR-065](0065-core-module-installed.md) carried the same error and is corrected there.

The doc-side copies of the same error were **missed on the first pass and are corrected now**, which is why the ledger is spelled out rather than summarized: two in [storage.md](../keeper/storage.md) (the `alias` and `schema` column comments), two in [plugins.md](../keeper/plugins.md) ("one column, not two" and the `schema` persistence note), and one in [naming-rules.md](../naming-rules.md) (the `plugin_sigils` row). Every surviving "113" beside `plugin_sigils` is now inside a sentence saying the line *used to* read 113; a bare "migration 113" anywhere else in the docs is the unrelated `113_subject_four_dimensions` (`rites`, [ADR-025](0025-augur.md)) and is correct.
