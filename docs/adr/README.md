# ADR — индекс архитектурных решений Soul Stack

Каталог ADR (Architecture Decision Records). Источник правды по верхнеуровневой архитектуре — [`docs/architecture.md`](../architecture.md); ADR постепенно выносятся из него в отдельные файлы (один ADR — один файл, `NNNN-<slug>.md`). Пока вынесены не все — для невынесенных ссылка ведёт на якорь внутри `architecture.md`.

Статусы: **active** — действующее решение; **amended** — действует, но дополнено Amendment-блоком(ами); **superseded** — заменено более новым ADR.

Всего **60 файлов** `NNNN-<slug>.md`, максимальный номер — **0063**. Нумерация **с пропусками**: номера 0034, 0036, 0037 не использованы (см. примечание в конце индекса). То есть «0001…0063» — не сплошной диапазон.

| ADR | Заголовок | Статус | Сводка |
|---|---|---|---|
| [001](0001-language-go.md#adr-001-язык-реализации--go) | Язык реализации — Go | active | Go как язык всех бинарей системы. **Вынесен в отдельный файл.** |
| [002](0002-transport-grpc-ha.md#adr-002-транспорт-keeper--souls--grpc-bidirectional-stream-поверх-mtls-ha-кластер-keeper) | Транспорт Keeper ↔ Souls — gRPC bidi поверх mTLS, HA-кластер | amended | Bidirectional stream поверх mTLS, инициирует Soul; Keeper — stateless-кластер (presence/shedding — amendments). **Вынесен в отдельный файл.** |
| [003](0003-destiny-format.md#adr-003-формат-destiny--yaml-с-типизированной-схемой-cuejson-schema) | Формат Destiny — YAML с типизированной схемой | active | YAML + JSON Schema/CUE, шаблонизатор отдельной фазой. **Вынесен в отдельный файл.** |
| [004](0004-binaries.md#adr-004-раскладка-бинарей--keeper-soul-soul-lint-push-режим--модуль-внутри-keeper) | Раскладка бинарей — keeper / soul / soul-lint | active | Три (затем четыре) артефакта; push — модуль внутри keeper. **Вынесен в отдельный файл.** |
| [005](0005-storage-postgres.md#adr-005-хранилище-состояния-keeper--postgres) | Хранилище состояния Keeper — Postgres | active | Postgres — единственное холодное хранилище состояния. **Вынесен в отдельный файл.** |
| [006](0006-cache-redis.md#adr-006-кэш-и-координация--redis) | Кэш и координация — Redis | amended | Redis — heartbeat, lease, pub/sub, лидер Reaper; presence Souls + Conclave; cluster-mode SSE/apply-events routing с шардированным каналом `events:shard:<n>` K=256 (amendments). **Вынесен в отдельный файл.** |
| [007](0007-versioning-git-ref.md#adr-007-версионирование-артефактов--через-git-ref-а-не-через-поле-в-манифесте) | Версионирование артефактов — через git ref | active | Версия артефакта = git ref, не поле в манифесте. **Вынесен в отдельный файл.** |
| [008](0008-coven-stable-tags.md#adr-008-coven--только-стабильные-логические-теги) | Coven — только стабильные логические теги | amended | Coven = стабильные метки; роль НЕ coven; окружение = частный случай coven (amendments). **Вынесен в отдельный файл.** |
| [009](0009-scenario-dsl.md#adr-009-scenario--полная-dsl-задач-destiny-граница-с-destiny--рекомендация) | Scenario — полная DSL задач destiny + оркестрация | amended | Scenario = все блоки destiny + оркестрационная дельта; cross-incarnation на Voyage-слое + опц. поле задачи `id:` + CEL-env прохода destiny видит read-only `soulprint.self.*` (hosts/where — scenario-only); per-task гранулярность `serial:`/§8 — через staged-render Passage ([ADR-056](0056-staged-render-passage.md)); грамматика `state_changes` (§7.1 `sets`/`appends`/`modifies` map) расширена до упорядоченного списка CRUD-глаголов ([ADR-057](0057-state-changes-crud-verbs.md)); top-level `compute:` — вычисляемые vars прогона (host-инвариантны, видны в `apply: input:` и `state_changes`); var→var внутри слоя `vars.*` — eager-topological резолв (цикл/unknown-ref → ошибка, порядок YAML безразличен, межслойная изоляция file↔task сохранена); условный `when:` на include-задаче — conditional-include = render-phase group-drop (реальное исключение группы из плана, только статич. `input.`/`essence.`/`incarnation.`/`vars.`-предикат; динамич. → `include_when_dynamic_unsupported`; сосуществует с block-static-skip); механизм нескольких именованных create-сценариев (декларация `create: true` per-scenario, удалён хардкод-дефолт `create`, выбор `create_scenario` обязателен при непустом наборе, bare-инкарнация при пустом — `created_scenario` NULLABLE, миграция 090); `extends: <covenant>` — наследование общего service-level контракта секций (`input`/`compute`/`state_changes`/`validate`) из `covenant.yml` корня service-репо: add-only shallow merge, fail-closed `section_key_conflict` (deep merge/last-wins отвергнуты), `form:` не мержится, append covenant-первым, резолв в `LoadScenarioManifestResolved` ДО потребителей (изоморфно `$type`/`include`), один extends / без рекурсии / только 4 секции, forward-compat (опционален), устраняет drift create↔create_from_souls (amendments). **Вынесен в отдельный файл.** |
| [010](0010-templating.md#adr-010-шаблонизатор-cel-для-yaml-выражений-go-texttemplate-для-файлов) | Шаблонизатор: CEL + Go text/template | amended | CEL — YAML-выражения, text/template — файлы; граница по файлам; CEL-env прохода destiny дополнительно регистрирует read-only `soulprint.self.*` (amendment 2026-06-18); var→var внутри слоя `vars.*` через CEL-AST `Engine.VarRefs` (eager-topological, amendment 2026-06-24). **Вынесен в отдельный файл.** |
| [011](0011-go-layout.md#adr-011-раскладка-go-кода-gowork-с-модулями-по-сторонам) | Раскладка Go-кода: go.work с модулями | active | go.work с семью модулями по сторонам. **Вынесен в отдельный файл.** |
| [012](0012-keeper-soul-grpc.md#adr-012-контракт-keepersoul-grpc-один-eventstream-с-oneof-keeper-side-рендер-forward-compat-only-add) | Контракт Keeper↔Soul gRPC | amended | Один EventStream с oneof, Keeper-side рендер, forward-compat only-add; back-link apply_run; dispatch-модель «один ApplyRequest на хост» → «N на хост по Passage» ([ADR-056](0056-staged-render-passage.md)) (amendments). **Вынесен в отдельный файл.** |
| [0013](0013-bootstrap-archon.md) | Bootstrap первого Архонта | amended | `keeper init --archon`, advisory-lock, первый Архонт cluster-admin; bootstrap-инвариант выражен через `created_via='bootstrap'` (amendment 2026-06-23). **Вынесен в отдельный файл.** |
| [0014](0014-operator-identity.md) | Identity-модель оператора (Archon) | amended | Реестр `operators`, JWT-credential; AID-charset расширен, near-instant revocation, поле `created_via` + перенос bootstrap-инварианта (amendments). **Вынесен в отдельный файл.** |
| [0015](0015-core-modules-mvp.md) | Core-модули MVP: точный список | amended | 17 Soul-side core (12 исходных MVP + пост-MVP `url`/`line`/`repo`/`firewall`/`http`). Registry-факт сейчас — 18 Soul-side (+`core.augur` по ADR-025) и 4 Keeper-side (`core.soul`/`core.cloud`/`core.vault` по ADR-017 + `core.choir` по ADR-044); сводка по факту — [module/README.md](../module/README.md#статус-каталога). Amendment 2026-06-18: `core.service` (systemd) перед мутирующими actions делает gated `daemon-reload` (param `daemon_reload` auto/always/never, default auto); новый state `core.file.directory` (params path/owner/group/mode/parents; идемпотентно, чинит drift owner/mode, поддерживает Scry). **Вынесен в отдельный файл.** |
| [0016](0016-parity-license.md) | Стратегия parity + лицензия | amended | Apache 2.0, open core; гибрид core-рерайт + community-плагины; SDK Фаза 2 (amendments). **Вынесен в отдельный файл.** |
| [0017](0017-keeper-side-core.md) | Keeper-side core расширены | amended | `core.cloud` (author-адрес `core.cloud.created`/`core.cloud.destroyed`, base+state — форма `core.cloud.provisioned` историческая, как адрес НЕ существует, amendment 2026-06-26) / `core.vault.kv-read`; cloud credentials-flow + 6 провайдеров (amendments). **Вынесен в отдельный файл.** |
| [0018](0018-soulprint-typed.md) | Soulprint typed-схема MVP | amended | `SoulprintFacts` вместо Struct-stub; `choirs` как факт, typed_facts byte-passthrough (amendments). **Вынесен в отдельный файл.** |
| [0019](0019-state-migration-dsl.md) | State_schema migration DSL | amended | Плоский DSL + CEL + foreach, forward-only, атомарная PG-tx; upgrade → drift (amendment). **Вынесен в отдельный файл.** |
| [0020](0020-plugin-infrastructure.md) | Plugin-инфраструктура | amended | manifest / handshake / lifecycle для трёх типов плагинов; SDK Фаза 2, SshProvider-набор (amendments). **Вынесен в отдельный файл.** |
| [0021](0021-hot-reload-config.md) | Hot-reload конфига с write-back YAML | active | Hot-reload конфига с перезаписью изменённого обратно на диск. **Вынесен в отдельный файл.** |
| [0022](0022-audit-pipeline.md) | Audit-pipeline | active | Storage / schema / retention аудит-журнала. **Вынесен в отдельный файл.** |
| [0023](0023-trial-test-runner.md) | Тест-раннер Trial + DSL-coverage | active | `soul-trial`, уровни L0–L3, метрика trial coverage. **Вынесен в отдельный файл.** |
| [0024](0024-observability.md) | Observability: Prometheus-primary + OTel-bridge | active | Prometheus-primary `/metrics` + OTel-bridge; префиксы `keeper_*`/`soul_*`. **Вынесен в отдельный файл.** |
| [0025](0025-augur.md) | Augur — брокер внешнего доступа Soul | active | Keeper-side брокер live-доступа Soul к внешним системам (Omen / Rite). **MVP-1 (брокер, `delegate=false`) реализован (2026-05), в baseline; MVP-2 (делегация) отложен** ([amendment 2026-06-16](0025-augur.md)). |
| [0026](0026-sigil.md) | Sigil — целостность плагинов | active | Keeper-signed digest-индекс целостности плагинов. **Вынесен в отдельный файл.** |
| [0027](0027-apply-work-queue.md) | Модель исполнения apply — work-queue + claim | amended | Acolyte-пул, Ward-claim, Summons; refuse-guard, recovery, GATE-1 deliver-once; reclaim/Ward-claim гранулярность → per-passage ([ADR-056](0056-staged-render-passage.md)) (amendments). **Вынесен в отдельный файл.** |
| [0028](0028-rbac-storage.md) | RBAC-storage → Postgres | amended | RBAC (роли/permissions/membership) в Postgres, фикс BUG-1; Amendment Synod (ADR-049). **Вынесен в отдельный файл.** |
| [0029](0029-service-registry.md) | Реестр Service-ов → Postgres | amended | Реестр Service-ов из `keeper.yml` в Postgres; закрывает остаток ADR-028(h); well-known ключи `keeper_settings` (`default_destiny_source`, `provisioning_allowed_methods`) — amendment (g). **Вынесен в отдельный файл.** |
| [0030](0030-vigil-oracle.md) | Vigil + Oracle — event-driven мониторинг | amended | beacons + reactor; S5 typed-payload / soul_beacon / inotify (amendments). **Вынесен в отдельный файл.** |
| [0031](0031-scry-drift.md) | Scry — drift-detection | amended | Declarative dry-run reconcile; Slice B/C, Plan-тираж, converge → operational (amendments). **Вынесен в отдельный файл.** |
| [0032](0032-push-orchestrator.md) | Push-orchestrator (Variant C) | amended | Multi-host destiny push без incarnation/scenario; серия S6/S7 + P2 routing (amendments). **Вынесен в отдельный файл.** |
| [0033](0033-errand.md) | Errand — pull-ad-hoc exec | amended | Pull-ad-hoc exec вне scenario; E5 cancel, Voyage command-kind (amendments). **Вынесен в отдельный файл.** |
| [0035](0035-distribution-split.md) | Distribution split — core vs web | amended | Разделение дистрибуции: core (API+CLI) vs web (UI). **Amended [ADR-055](0055-embed-ui-bundle.md)** — отложенный embed-compat-shim активирован как опциональный default-ON embed UI на `/ui` (бета single-binary onboarding). **Вынесен в отдельный файл.** |
| [0038](0038-toll.md) | Toll — detector массового оттока Souls | amended | Cluster-wide detector оттока Souls; webhook + per-coven thresholds (amendment). **Вынесен в отдельный файл.** |
| [0039](0039-e2e-testing.md) | E2E-тестирование — три уровня | amended | Три уровня e2e без новой сущности словаря; L3a-impl (amendment). **Вынесен в отдельный файл.** |
| [0040](0040-tide.md) | Tide — invocation-time scope chunking | superseded | Поглощён [ADR-043 (Voyage)](0043-voyage.md#adr-043-voyage--унифицированный-батчевый-прогон) режимом `kind=scenario`. **Вынесен в отдельный файл.** |
| [0041](0041-errandrun.md) | ErrandRun — multi-target обвязка над Errand | superseded | Поглощён [ADR-043 (Voyage)](0043-voyage.md#adr-043-voyage--унифицированный-батчевый-прогон) режимом `kind=command`. **Вынесен в отдельный файл.** |
| [0042](0042-backend-driven-ui.md) | Backend-driven dynamic data в UI | amended | UI не хардкодит динамические каталоги — фетчит из backend. **Вынесен в отдельный файл.** Amend: инстанс `GET /v1/event-types` (Tiding-каталог, ADR-052). |
| [0043](0043-voyage.md) | Voyage — унифицированный батчевый прогон | active | Унифицированный батчевый прогон; поглощает Tide (ADR-040) и ErrandRun (ADR-041). **Вынесен в отдельный файл.** |
| [0044](0044-choir.md) | Choir — именованная топология хостов | active | Именованная позиция хоста внутри инкарнации; стабильный declared-факт. **Вынесен в отдельный файл.** |
| [0045](0045-param-dsl.md) | Param-DSL модулей | active | Типизированные input-поля модулей для UI-формы Run Command. **Вынесен в отдельный файл.** |
| [0046](0046-cadence.md) | Cadence — регулярные запуски | active | Scheduled/recurring Voyage (interval + cron). **Вынесен в отдельный файл.** |
| [0047](0047-purview.md) | Purview — scoped RBAC-видимость узлов | active | Role default_scope + расширенный селектор видимости узлов. **Вынесен в отдельный файл.** |
| [0048](0048-conductor.md) | Conductor — исполнитель Cadence-расписаний | active | Leader-elected исполнитель Cadence-расписаний. **Вынесен в отдельный файл.** |
| [0049](0049-synod.md) | Synod — группа архонов | active | Группа архонов, бандлящая роли (уровень Архон → Synod → Роли); амендит ADR-028. **Вынесен в отдельный файл.** |
| [0050](0050-tempo.md) | Tempo — per-AID rate-limiting write-API | active | Per-AID rate-limiting на write-API. **Вынесен в отдельный файл.** |
| [0051](0051-operator-api-codegen.md) | Operator API codegen: OpenAPI → Go-типы | superseded | oapi-codegen: types-only → strict-server; упразднение `proto/operator/v1`; S6 мост-паттерн. **Заменён [ADR-054](0054-openapi-code-first.md)** (разворот spec-first → code-first); **реализация СНЕСЕНА 2026-06-13 (HEAD `fde65bf`)** — `oapi`-пакет/`oapi_strict.go`/рукопись-источник/`gen-api`/`check-gen-api` удалены. **Вынесен в отдельный файл.** |
| [0052](0052-herald-notifications.md) | Herald + Tiding — уведомления о событиях прогонов | amended | Herald (канал) + Tiding (правило подписки); tap поверх audit-writer, at-least-once webhook-доставка, scope = события прогонов; ephemeral-Tiding + гибкое тело annotations/projection (amendment). **Вынесен в отдельный файл.** |
| [0053](0053-dependency-tiers.md) | Tier-ы инфраструктурных зависимостей | active | Обязательный контур PG + Redis + Vault (все fail-fast); Vault hard-required (старт/auth/PKI); OPTIONAL-with-degradation tier; без-Vault режим отвергнут. **Вынесен в отдельный файл.** |
| [0055](0055-embed-ui-bundle.md) | Embed UI bundle — опциональный single-binary keeper с UI на `/ui` | active | Опциональный (default-ON) go:embed завендоренного build-снапшота soul-stack-web в keeper; маршрут `/ui` + SPA-fallback, публичная статика (parity `/docs`), тоггл `web_ui_enabled`, делит listener `:8080`; синк `sync-webui.sh` + drift-guard `check-webui`. **Amends [ADR-035](0035-distribution-split.md).** **Вынесен в отдельный файл.** |
| [0054](0054-openapi-code-first.md) | Operator API — разворот на code-first (Go → OpenAPI) через huma | amended | Заменяет ADR-051 (инверсия spec-first→code-first); Go-типы — источник, спека производна (huma v2 + humachi); **FULL-TYPED** (Amendment 2026-06-12: typed input/output + извлечённая `XTyped`, `RawBody`-мост отвергнут). **ТИРАЖ ЗАВЕРШЁН 2026-06-13 (HEAD `fde65bf`)**: все ~19 доменов handler-native, served=runtime huma-dump (`HumaFullSpecYAML`), committed `openapi.yaml` производный (`gen-openapi`/`check-openapi`), oapi-каркас снесён, native enum-каталог `huma_enums.go`. **Amendment 2026-06-15**: визуальный вьювер `GET /docs` (RapiDoc, go:embed) + served-spec за JWT (`GET /openapi.yaml`/`/openapi.json`) — механизм A. **Вынесен в отдельный файл.** |
| [0056](0056-staged-render-passage.md) | Staged-render — прогон сценария как N упорядоченных Passage | active | Прогон = N упорядоченных **Passage** (render→dispatch→barrier→сбор register); реализует probe→where (`register` в `where:`/`apply:input` следующих задач) реально, закрывает doc-drift orchestration.md §4/§5. Топологический N-stage по register-зависимости; state-commit один раз после последнего Passage (§7 не дробится). proto only-add `passage`; PG-PK per-passage; explicit-reject старого Soul. **Amends [009](0009-scenario-dsl.md) / [012](0012-keeper-soul-grpc.md) / [0027](0027-apply-work-queue.md).** **Вынесен в отдельный файл.** |
| [0057](0057-state-changes-crud-verbs.md) | `state_changes` — упорядоченный список CRUD-глаголов | amended | `state_changes` → упорядоченный **список** операций (не map); глаголы `set`/`add`/`modify`/`remove` + `foreach`/`as`/`do` (reuse migration-DSL). Множественность через `match`-предикат (не ручки/флаги); биндинги `elem`/`key`/`value`; опц. `expect` (one/at_most_one/any); `add` с `on_conflict` (skip/replace/error, default skip = идемпотентно); soul-lint WARN на широкий match, empty-match → no-op. Чинит латентный баг (`appends`/`modifies` были no-op — state не рос). `remove`≠migration-`delete`. Transit: dual-parse map-формы один релиз (deprecated). **Amends [009](0009-scenario-dsl.md).** Amendment 2026-06-24: day-2-сценарии читают развёрнутый факт из `incarnation.state`, НЕ из `essence`/`input` (read-конвенция симметрична write-семантике; спека — [production-conventions §7a](../destiny/production-conventions.md)). **Вынесен в отдельный файл.** |

| [0058](0058-operator-auth-ldap-oidc.md) | Федеративная аутентификация операторов — LDAP + OAuth2/OIDC | accepted + end-to-end (LDAP + OIDC) | Внешняя auth валидируется на Keeper-е → маппится на `operators(aid)`+роли → выпускается ВНУТРЕННИЙ JWT (RBAC/MCP/middleware без изменений). **LDAP реализован end-to-end (стадия 1):** search-bind (`go-ldap/v3`), auto-provision по группам, роли из `group_role_map`→`rbac_role_operators`, aid из `aid_attr` (дефолт `uid`), JWT в HttpOnly+Secure+SameSite=Strict cookie `soul_session` (JSON-токена нет), no-refresh, revoked-инвариант; `POST /auth/ldap/login` вне `/v1`. **OIDC реализован end-to-end (стадия 2):** authorization-code flow с **обязательным PKCE (S256)**, discovery (`go-oidc/v3`)+JWKS-валидация id_token (подпись/iss/aud/exp/nonce), Redis flow-state store (state→nonce/verifier, single-use GETDEL, TTL 5m), `GET /auth/oidc/{login,callback}` вне `/v1`, JWT в cookie `soul_session` (`SameSite=Lax`), общий `DBMapper` (Method=oidc). Расширение `auth_method` enum (`ldap`/`oidc`, only-add, миграция 083); конфиг-блоки `auth.ldap`/`auth.oidc` (секреты — Vault `*_ref`); TLS-required. **Блокер посева `archon-system` ЗАКРЫТ** (amendment 2026-06-23): поле `operators.created_via` + релакс bootstrap-индекса на `WHERE created_via='bootstrap'` (миграции 084/085), `archon-system` посеян миграцией 086, federated пишет `created_by_aid=NULL`+`created_via='ldap'`\|`'oidc'` (костыль `FederatedSourceAID` удалён). **Политика провижининга (i):** `provisioning_allowed_methods` + `/v1/provisioning-policy` (perm `provisioning.read`/`provisioning.update`, audit `provisioning.policy_changed`, problem `provisioning-method-disabled`). **Amends [ADR-014](0014-operator-identity.md) / [ADR-013](0013-bootstrap-archon.md) / [ADR-029](0029-service-registry.md).** **Вынесен в отдельный файл.** |

| [0059](0059-audit-sink-pluggable.md) | Pluggable audit sink — PG / Kafka / off | proposed / deferred | **Design-only, не реализовано (план пост-беты).** Backend audit-выгрузки — выбор реализации `shared/audit.Writer` (абстракция уже есть, `MultiWriter`-tap сохраняется), врезка в `setupAudit`. `keeper.yml → audit.sink: pg \| kafka \| off` (default `pg` = текущий PG-`audit_log`, обязательный контур ADR-053 цел). `kafka` — новый sink в `keeper/internal` (`shared` остаётся pgx-free/Kafka-free): at-least-once `acks=all`, деградация **fail-closed** (audit compliance-критичен), дедуп downstream по `audit_id` (ULID PK). Tier: Kafka строго **OPTIONAL-with-degradation**, НЕ 4-й required. Переключение sink — **restart-required** (паттерн `web_ui_enabled`). Вытесняет Redis-Stream-вариант backlog-а audit-scaling, batched-INSERT остаётся (более дешёвая альтернатива на оси write-throughput). **Hard-зависимость ДО реализации (open question (n)):** `changed_tasks`/`incarnation.run_completed` ([ADR-052 §k](0052-herald-notifications.md)) и `GET /v1/audit` сегодня деривят данные SQL-запросом по `audit_log` в PG — при `sink: kafka` без PG они молча ломаются; нужен альтернативный источник событий (3 кандидата). Рабочее имя `audit sink`; тематическое **Chronicle** — кандидат-альтернатива, в naming-rules НЕ зафиксирован. **Amends [ADR-022](0022-audit-pipeline.md) (указатель) / [ADR-053](0053-dependency-tiers.md) (OPTIONAL-строка).** **Вынесен в отдельный файл.** |

| [0060](0060-traits.md) | Trait — operator-set key-value метки на инкарнации (релоцировано с Soul) | amended | Trait = key-value (scalar\|list) метки оператора, ОТДЕЛЬНАЯ ось рядом с плоским Coven (Вариант B; расширение `souls.coven` до key-value отвергнуто — ломает ADR-008 + scope-pushdown `$1=ANY(coven)` + `'x' in covens`). **R1 (2026-06-25): Trait РЕЛОЦИРОВАН per-soul → per-incarnation** — источник истины `incarnation.traits jsonb` (operator-set в `incarnation.spec.traits`, миграция `088`, зеркало `incarnation.covens`/`046`), проецируется МАТЕРИАЛИЗОВАННО в `souls.traits` хостов-членов через **sync-hook** (`SyncTraitsToHosts`, переиспользует `soul.BulkReplaceTraits`; врезка create + bind `core.soul.registered`). `souls.traits` (`087`) = projection target, read-слой переиспользуется (`soulprint.self.traits` / `where:traits` / soul-lint / topology — 0 изменений). Таргетинг `where: soulprint.self.traits.<key>` (CEL `DynType`, без AST-rewrite). **R1 slice a (2026-06-25): API-релокация per-soul→per-incarnation РЕАЛИЗОВАНА** — `incarnation.spec.traits` на create + day-2 `PUT /v1/incarnations/{name}/traits` (permission `incarnation.traits-set`, audit `incarnation.traits_changed` — только КЛЮЧИ, MCP `keeper.incarnation.traits-set`); per-soul write-путь `POST /v1/souls/traits` / `soul.traits-assign` / `keeper.soul.traits-assign` **DEPRECATED, НЕ удалён** (forward-compat, перетирается проекцией). RBAC-scope по traits на incarnation-измерении РАЗБЛОКИРОВАН (реализация — следующий слайс). **Amends [ADR-008](0008-coven-stable-tags.md) / [ADR-018](0018-soulprint-typed.md).** **Вынесен в отдельный файл.** |
| [0061](0061-onboarding-await-and-midrun-reresolve.md) | Единый-прогон provision→онбординг→роль: onboarding-await + mid-run re-resolve | amended | Один create-scenario: provision (`core.cloud.provisioned`) → ожидание онбординга созданных Souls → mid-run рост roster → применение роли. Две способности на существующем `core.soul.registered` (НЕ новый модуль — решение пользователя). **(1) onboarding-await:** новые флаги `await_online`/`await_timeout`(required-when)/`await_min_count`/`await_poll_interval` — после register+coven шаг блокирующе поллит **Redis SID-lease** (источник истины online, НЕ PG status) до `await_min_count`/timeout; **B1-strict** (online<min к таймауту → `failed` → state не коммитится → `error_locked`); output `online[]`/`pending[]`/`satisfied`; потолок `keeper.yml::max_await_timeout` (DoS-guard, fail-closed). list-SID: `sid` принимает строку ИЛИ список (барьер агрегирует по всем). **(2) mid-run re-resolve:** оживлён `refresh_soulprint` — после успеха шага runner пере-резолвит roster инкарнации перед СЛЕДУЮЩИМ Passage (монотонный рост, только +хосты); инвариант roster-стабильности ослаблен до per-Passage. **Stratify:** `refresh_soulprint` делает шаг passage-определяющим (потребители `soulprint.hosts`/`on:[incarnation.name]` уезжают в следующий Passage). **HA:** provision-сценарии через Voyage (standalone staged-recovery открыт). **S1 (await) реализован; S2 (stratify)/S3 (re-resolve в run.go) — контракт зафиксирован, код отдельными слайсами.** **Amendment 2026-06-28 (no_hosts-bypass, два класса):** no_hosts-гейт на пустом roster пропускается при одном из двух признаков provision-from-zero — (а) all-keeper (ВСЕ задачи `on: keeper`, `core.cloud.created` создаёт VM С НУЛЯ; по СОСТАВУ через `allKeeperTasks`, не флаг), (б) mixed с refresh-эмиттером (`config.HasRefreshEmitter`: план несёт `core.soul.registered` с `refresh_soulprint: true` → roster пере-резолвится mid-run §S2/§S3, host-задачи деплоя уезжают в Passage после refresh-границы → видят онбордившиеся VM); host-only и mixed-БЕЗ-refresh держат no_hosts; Essence на пустом roster резолвится в keeper-контексте. **Amendment 2026-06-29 (size-asserts-гейт):** size-asserts деплой-веток redis (`create/cluster.yml`/`create/sentinel.yml`/`migrate_cluster/cluster.yml`) гейтятся `when: "!(has(input.provision) && input.provision.enabled)"` — при provision (souls ещё не созданы) STATIC-предикат placeholder-skip-ает assert на pre-flight (иначе `size(soulprint.hosts)==N` падал бы 422 на пустом roster ДО провижна); roster-инвариант на provision-пути держат `count`-формула `redis-provision.yml` + `await_online`-барьер, НЕ pre-flight; на существующем roster-е size-guard активен как раньше; pre-flight остаётся не-staged (правится сервис-DSL, не `EvalAsserts`). Симметрично no_hosts-bypass. **Amends [ADR-009 §7](0009-scenario-dsl.md) / [ADR-056](0056-staged-render-passage.md) / [ADR-006](0006-cache-redis.md) / [ADR-017](0017-keeper-side-core.md).** **Вынесен в отдельный файл.** |
| [0062](0062-input-types.md) | Named input types — переиспользуемые именованные схемы input | active | Переиспользуемые именованные input-схемы: секция `types:` в `service/<name>/types.yml` (тот же InputSchema-DSL) + ссылка `$type: <Имя>` как поле или `items: {$type}` для массива. **Заменяет нереализованный `$ref`/`schemas/`** (удалён из architecture.md/manifest.md). Резолв service-level (НЕ local-per-scenario, НЕ кросс-сервис); MVP = object + array-of-type + вложенность тип→тип с **обязательным cycle-detection**; БЕЗ scalar-alias/generics/кросс-сервис. Ошибки `input_type_unknown`/`input_type_cycle`/`input_type_duplicate`/`input_type_ref_conflict`. DTO `/v1/scenarios` резолвит `$type` backend-side ДО проекции + аннотация `x-type: <Имя>` (forward-compat UI-виджет). Wave 1 = механизм на текущем `AclUser`. **Amends задел `$ref` в [ADR-003](0003-destiny-format.md) / [ADR-009](0009-scenario-dsl.md).** **Вынесен в отдельный файл.** |
| [0063](0063-bootstrap-token-delivery.md) | `core.bootstrap.delivered` — keeper-side доставка bootstrap-токена по SSH | active | Новый keeper-side core-модуль (`on: keeper`) тонкой доставки per-VM bootstrap-токена на свежесозданные cloud-init-VM. **Закрывает BUG#2 cloud-provision**: заглушка-адрес `keeper.push.applied` keeper-side не существовала → созданная VM не получала токен → барьер `await_online` не набирал presence → `error_locked`. **Дизайн A1 «тонкая доставка»:** cloud-init (B-flat) уже поставил soul-бинарь+CA+unit; модуль кладёт ТОЛЬКО токен (`/etc/soul/token`, umask 077, 0400, **★токен в STDIN не в argv**) + опц. `systemctl start soul`. Поток per-host (последовательно): `Authorize` (deny→fail-closed) → ephemeral keypair+`Sign` → `push.Dial` (CA-signed host-cert verify) → write token → опц. start. **B1-strict** (ошибка любого хоста → `failed`). Params: `hosts` (`${ register.provision.hosts }`), `ssh_provider`, `token_path`/`ssh_user`/`ssh_port`/`start_soul` (default-ы). Output `hosts[]={sid,delivered,started}`+`count` — **БЕЗ токена**; audit `bootstrap.delivered` — `{count, sids}` без токенов. Переиспользует `keeper/internal/push` (ephemeral/Sign/Dial/Session). Регистрация **условна** (как `core.choir`): при полном наборе SSH-deps (provider+host-CA+dialer). **Границы MVP:** один key-based provider, только токен, хосты последовательно. **★ C1 — cloud-init CA-signed host-key (required-для-live, СЛЕД. слайс):** до него `push.Dial` реджектит host-cert свежей VM (голый host-key) → live-e2e не пройдёт (модуль валиден на render+unit). **Amendment (Teleport by-name transport):** второй режим `transport: teleport` (vs default `direct`) — by-name через Teleport Proxy (target=SID, НЕ primary_ip), транспорт+auth+host-verify через Teleport identity-file (Authorize/Sign/Vault-host-CA НЕ нужны, C1 неприменим), retry-до-join (`join_wait_timeout`); creds — `keeper.yml::push.teleport`, плагин в флоу не участвует. **Amendment 2026-06-30 (full-install режим):** два режима доставки — **token-only** (cloud-init поставил setup, текущее) и **full-install** (delivered ставит ВЕСЬ setup по Teleport SSH — для платформ без cloud-init userdata, напр. WB namespace без `ci_user_data`); install-blueprint вынесен в shared [keeper/internal/soulinstall](../../keeper/internal/soulinstall) (canonical `Blueprint` + `RenderCloudInitYAML` userdata + `RenderInstallScript` SSH-шаги, DRY); источник blueprint = `keeper.yml::cloud_init` (config-reuse); secret-write через SSH stdin (не argv) в обоих режимах; слайсы 1=blueprint-вынос (готов)/2=install-режим/3=scenario+live; cross-ref [ADR-017(h)](0017-keeper-side-core.md) (`generate_userdata` НЕ единственный путь онбординга). **Amends [ADR-017](0017-keeper-side-core.md) / [ADR-061](0061-onboarding-await-and-midrun-reresolve.md) / [ADR-015](0015-core-modules-mvp.md).** **Вынесен в отдельный файл.** |

> ADR-034, ADR-036, ADR-037 — номера не использованы (gap в нумерации).
