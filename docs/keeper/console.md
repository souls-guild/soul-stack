# Keeper — interactive console plane (`/v1/console`)

The Keeper half of Multi-console: the operator's WebSocket, the session manager
that maps it onto the Keeper↔Soul EventStream, and the backpressure that keeps a
flooding terminal from touching anything else.

The decision record is [ADR-0074](../adr/0074-interactive-console-pty.md); this
document is the Keeper-side spec it points to for the frame contract. The Soul
half — the pty itself, its host policy and flow control — is
[soul/console.md](../soul/console.md). All three share one wire vocabulary
(`console_*`), defined in `proto/keeper/v1/console.proto`.

Implementation: `keeper/internal/console/` (session manager) +
`keeper/internal/api/console_ws.go` (the endpoint). Client mirror:
`soul-stack-web/src/api/consoleProtocol.ts`.

## 1. Why a WebSocket

It is the only WebSocket in Keeper; every other surface is HTTP/1.1 + SSE. A
console genuinely needs the shape: SSE is one-directional and cannot carry
keystrokes back, and a POST per keypress on a `top` session is not a terminal.

The endpoint is deliberately **absent from the OpenAPI spec**. OpenAPI models a
request/response pair, while an upgrade replaces the response with a raw socket;
documenting it as a `GET` returning 101 would describe a contract no generated
client can call. The real contract is the JSON frames below. A guard in
`huma_full_spec_test.go` pins the exemption so no OTHER route can drift out of
the spec unnoticed.

## 2. Connection

```
GET /v1/console
Upgrade: websocket
Sec-WebSocket-Protocol: soul-stack.console.v1, bearer.<jwt>
```

Keeper selects and echoes back `soul-stack.console.v1`. A handshake that does not
offer it is refused with 400 — gorilla only echoes a subprotocol it was offered,
and a browser silently closes a socket whose negotiated subprotocol is missing,
which would look like a server that accepted and then hung up.

**Auth rides in the subprotocol**, not a query parameter: the browser
`WebSocket` constructor takes only a URL and a subprotocol list, so an
`Authorization` header is impossible. The subprotocol is preferred over the
query-token the SSE stream has to live with (ADR-068 §A0) because it keeps the
JWT out of access logs, `Referer` and browser history. The exception is scoped to
a genuine upgrade (`middleware.isWebSocketUpgrade`) — on a plain GET the same
header is ignored, or it would become a general header-shaped bypass.

### Permission

Two gates, because the target host is not in the URL:

| Gate | Where | Question |
|---|---|---|
| `soul.console` — **existence** (`HoldsAction`) | chi middleware, before the upgrade | May this Archon open consoles at all? Refusal is **HTTP 403**, before any socket exists. |
| `soul.console` + `host=<sid>` — **scope-aware** (`Check`) | in-handler, per `open` frame | May they open one on THIS host? Refusal is a session-scoped `error` frame; the socket and its other panes stay live. |

The upgrade gate is `RequireAction`, **not** `RequirePermission` with an empty
selector ([ADR-0074(c)](../adr/0074-interactive-console-pty.md),
[ADR-047 §g G1](../adr/0047-purview.md)). A scope-aware `Check` with an absent
dimension fails closed, so it would deny precisely the `host=`-scoped roles the
feature exists to serve: the operator would be refused the socket before ever
naming a host. Pinned on both sides — `TestEnforcer_ConsoleSocketGateIsExistenceOnly`
in the RBAC package and the host-scoped upgrade test in `console_ws_test.go`.

The permission lives in the closed catalog (`keeper/internal/rbac/catalog.go`,
[rbac.md §Console](rbac.md)). Note the deliberate consequence recorded in
ADR-0074(c): `soul.*` covers `soul.console`, so a role already holding the
resource wildcard gains shell access — an operator who must not hand out shells
enumerates actions instead.

## 3. Frames

All frames are JSON text. Byte payloads (`data`) are **base64**: a pty carries
arbitrary bytes (ANSI escapes, partial UTF-8 runes, NUL) and JSON cannot hold
them raw. Fields mirror the `console_*` proto oneofs 1:1, so Keeper relays rather
than translates.

One socket multiplexes N sessions. **`session_id` on this wire is generated by
the client** so it can correlate `open` with `opened` without a round trip; it is
unique only within its socket. Keeper mints its **own ULID** for the Soul side,
where uniqueness must hold per EventStream — two operators would otherwise
collide on "pane-1" against the same host. The translation is the session
manager's job and neither id ever leaks into the other half of the system.

### Client → Keeper

| type | fields | notes |
|---|---|---|
| `open` | `session_id`, `sid`, `cols`, `rows`, `shell?` | `shell` must be an absolute path (the Soul refuses a relative one). |
| `stdin` | `session_id`, `data` | base64; per keystroke, never batched into lines. |
| `resize` | `session_id`, `cols`, `rows` | → `TIOCSWINSZ` → `SIGWINCH`. A zero dimension is dropped, not forwarded. |
| `close` | `session_id` | Ends one pane; the socket's other sessions stay live. |

### Keeper → Client

| type | fields | notes |
|---|---|---|
| `opened` | `session_id`, `sid`, `pid?` | Until this arrives the pane is "connecting" and refuses input. |
| `chunk` | `session_id`, `stream` (`stdout`\|`stderr`), `data`, `dropped_bytes?` | base64. Chunk boundaries are free — the client re-joins split runes. |
| `exit` | `session_id`, `code`, `reason?`, `message?` | The single terminal frame of a session. |
| `error` | `session_id?`, `code`, `message` | With `session_id` — that pane only; without — socket-scoped. |

`code` is a **number** on `exit` and a **string** on `error`; the client's
discriminated union depends on it.

`reason` on `exit` mirrors `ConsoleExitReason` in snake_case: `process_exited`,
`closed_by_keeper`, `open_failed`, `soul_shutdown`, `limit_exceeded`. It matters
because an exit code of `0` from a shell the operator never closed reads very
differently from a clean `exit`.

`error.code` is a closed set: `bad_frame`, `forbidden`, `soul_offline`,
`soul_busy`, `console_unsupported`, `limit_exceeded`, `duplicate_session`,
`internal`.

Unknown frame types are ignored by the client, so Keeper may add frames later
without breaking an older UI.

## 4. Backpressure

Console output must never become Keeper's memory or stall anything else. Two
properties are non-negotiable:

- **Delivery is non-blocking.** Frames arrive on the EventStream goroutine, which
  the apply cycle shares. A console that could block there would stall
  `TaskEvent`/`RunResult` for that host.
- **Loss is visible.** A bounded queue means a flooding terminal loses output;
  hiding that would splice a corrupted ANSI stream and the operator would read
  a screen that never existed.

So each socket has one bounded queue (256 frames) drained by a single writer:

- **chunks** are dropped when it is full, and their byte count accumulates per
  session;
- **lifecycle frames** (`opened`/`exit`/`error`) are never dropped — losing an
  `opened` strands a pane on "connecting", losing an `exit` leaves it live
  forever. A full queue at that point means the peer stopped reading entirely, so
  the socket is closed and its sessions reaped;
- the accumulated drop count is reported in `dropped_bytes` on the next chunk,
  and — if the flood has ENDED and there is no next chunk — flushed within 250 ms
  as a marker chunk with empty `data`.

`dropped_bytes` sums BOTH stages: the Soul-side token bucket
(`ConsoleChunk.dropped_bytes`, 1 MiB/s per session) and this queue.
`keeper_console_dropped_bytes_total` against `soul_console_dropped_bytes_total`
says which side is the bottleneck.

### The other direction: keystrokes

Downstream has a bound too, and it is not the console's own: `ConsoleStdin`
shares the per-SID outbound queue (depth 10) with the apply cycle. A burst that
overflows it is refused with `error{code: "soul_busy"}` — deliberately NOT
`soul_offline`, since the host is healthy and the operator should retry rather
than go hunting for a dead agent.

The defaults are self-consistent: the Soul-side host cap is 8 consoles, so even
every terminal on one host typing at the same instant stays under the queue. It
takes a synthetic burst — more consoles on ONE host than the queue is deep, all
sending at once — to hit it. Raising `console.max_sessions` on a host well past 8
is what would make this reachable in practice.

## 5. Lifecycle

### Kill on disconnect

**A console never outlives the socket that authorized it.** When the socket
drops — a close frame, a dead TCP connection, a failed ping — every session
behind it gets a `ConsoleClose` and the Soul kills the whole process group. This
mirrors the Soul-side invariant (a console never outlives its EventStream): an
orphaned interactive root shell is the worst failure this feature can produce.

The client acts on the same rule — it marks every session closed on disconnect
and never resumes; a reconnect opens *fresh* shells.

Liveness is detected by ping/pong (60 s), not by TCP: a laptop that slept keeps a
half-open connection alive for many minutes, and every pty behind it with it.

### Idle timeout

A session with no operator **input** for `idle_timeout` (default 30 m) is closed.
Output does not count as activity — a `tail -f` left running overnight is exactly
the abandoned root shell this reaps.

This is Keeper-side on purpose: it is operator policy, and the host has no idea
whether anyone is watching.

### Terminal semantics

The Soul sends exactly one `ConsoleExit` per session it was asked to open,
including for a refused open. So Keeper can free its state unconditionally on the
terminal, and an operator `close` does **not** synthesize an `exit` frame —
inventing a second terminal would show the client an exit code no shell produced.

## 6. Cross-Keeper routing

Keeper is a stateless cluster (ADR-002), and a console spans two connections that
land independently: the operator's socket on whichever instance the load balancer
picked, the host's EventStream on whichever won the SoulLease. With N instances
they coincide about 1/N of the time — so this is the normal path, not an edge.

**Downstream** (keystrokes) needs nothing new: `Outbound` already routes a
`FromKeeper` to the lease holder over `outbound:<sid>`.

**Upstream** (pty output) is addressed by owner:

1. the socket owner claims `console:owner:<session_id> → <kid>` (TTL 15 min,
   refreshed every 5 min while the session lives);
2. the stream holder, seeing a session it does not hold, resolves that claim
   **once** and caches it — a Redis GET per pty chunk would put the cluster cache
   in the path of every keystroke echo;
3. it republishes the frame to `console:<owner-kid>` — one channel per instance,
   subscribed once at startup rather than one subscription per open terminal.

A frame that arrived over the bridge is delivered locally or dropped, never
re-forwarded: otherwise a stale route would bounce it between instances.

With no Redis this degrades to single-instance operation: a console whose socket
and stream landed together still works.

### When the socket's Keeper dies

Kill-on-disconnect covers the socket dying. It does NOT cover the Keeper that
HELD that socket dying — and that gap is worse than it sounds: the EventStream
never broke, so the Soul-side invariant never fires either, and the pty keeps
running as an interactive root shell with nobody left watching it. A live
two-keeper stand confirmed a shell surviving its owner's death.

The instance holding the stream is the only party that can still see the
session, so it is the one that reaps. It detects the orphan two ways, and both
are needed:

- **on the forward path** — a frame arrives for a session whose claim is gone or
  whose owner is no longer subscribed;
- **by an active sweep** every 30 s over the sessions it is bridging. This is
  the one that matters: the dangerous session is the QUIET one. An operator who
  ran `sleep 900`, or simply left a shell at its prompt, produces no output at
  all, so a purely reactive check would never fire.

The claim TTL (90 s, refreshed every 30 s) is therefore the detection window: an
abandoned shell dies within about two minutes rather than surviving until
someone notices. The two paths share a reap mark, so one orphan produces one
`ConsoleClose`.

A Redis failure yields NO orphans. A lookup that errored says nothing about the
owner, and killing a healthy operator's shell over a cache blip would be far
worse than the blip.

## 7. Limits

Keeper-side limits are **operator** policy, distinct from the **host** policy in
`console:` of `soul.yml` (`max_sessions: 8` per machine). A host cannot answer
"how many terminals may this Archon hold" — a wall of 30 hosts is one session on
each of them.

| `keeper.yml` → `console:` | Default | Meaning |
|---|---|---|
| `max_sessions_per_archon` | 30 | Live consoles one Archon may hold. Covers the walls the UI is built for; past that an operator is not reading output, they are running a fan-out — which is what an Errand is for. |
| `max_sessions_global` | 256 | Live consoles on ONE Keeper instance, across all operators. |
| `idle_timeout` | `30m` | Close after this long without operator input. `0s` disables. |

Exceeding either cap gives `error{code: "limit_exceeded"}` on that pane only.

Frame-plane constants (queue depth 256, ping 60 s, max inbound frame 1 MiB) stay
in code: they are flow-control tuning with no operator-visible policy meaning.

## 8. Capability gate

Keeper checks the Soul's announced `console` capability
([soul-capabilities](../naming-rules.md)) **before** minting a session and
refuses with `console_unsupported` if it is absent. Fail-closed, like `passage`:
an older binary drops `ConsoleOpen` into the default branch of its recv-loop and
never answers, which would leave the operator watching a dead terminal until the
idle timeout.

## 9. Audit and metrics

Sessions are audited as facts — `console.opened` / `console.closed`, with
`archon_aid`, the target `sid` and the session id as `correlation_id`. WHO opened
a shell WHERE belongs in the audit log even where session recording (NIM-145) is
off, because a console is the most privileged thing an operator can do: the pty
inherits the Soul daemon's user, typically root, and its commands cannot be
checked against a module allow-list the way an Errand's can.

| Metric | Meaning |
|---|---|
| `keeper_console_sessions_active` | Live sessions on this instance. The leak signal — it must return to zero after every operator disconnects. |
| `keeper_console_sessions_total{reason}` | Terminal sessions by `ConsoleExitReason`, plus `orphan_reaped` for a console killed because its socket's Keeper died. |
| `keeper_console_output_bytes_total` | Output forwarded toward operators. |
| `keeper_console_dropped_bytes_total` | Output dropped by Keeper-side backpressure. |
| `keeper_console_sockets_active` | Open operator WebSockets. |

## 10. Known gaps

- **Recording** — keystrokes and output are not persisted; session recording is
  NIM-145.
- **A holder restart mid-session** leaves the owner's pane with no terminal
  frame: the stream broke, so the pty is already dead (kill-on-disconnect), but
  the operator learns only from the idle timeout. A liveness probe from the owner
  side is a follow-up. (The mirror case — the OWNER dying — is handled, see
  §6 "When the socket's Keeper dies".)
- **Cross-instance chunk loss** is silent in one case: a bridged frame dropped by
  a congested Redis forward channel is logged but not counted in
  `dropped_bytes`, since the accounting lives on the socket side.
