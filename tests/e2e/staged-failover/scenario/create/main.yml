name: create
description: |
  Тривиальный auto-create scenario (POST /v1/incarnations авто-запускает create).
  Один шаг без register/where — staged-render тут не задействован (N=1). Реальный
  2-passage proof — в scenario/failover, запускаемом отдельным RunScenario.
state_changes: {}
tasks:
  - name: Init marker
    module: core.exec.run
    changed_when: "false"
    params:
      cmd: init-marker
