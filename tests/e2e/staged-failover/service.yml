name: staged-failover
state_schema_version: 1
description: |
  Staged-render (ADR-056) 2-passage e2e fixture: probe role → action on master.
  NOT in examples/**: a test fixture staged-render-proof (examples/ is the
  user's WIP zone). Reproduces the canonical probe→where idiom
  (orchestration.md §4-5 / ADR-008 volatile role) on the contract tier.
state_schema:
  type: object
  properties: {}
