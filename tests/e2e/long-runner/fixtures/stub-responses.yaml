# Response script for soul-stub for long-runner::create.
#
# scenario writes an init marker /tmp/long-runner-init.txt via core.file.present.
# state_changes.sets: last_run="create", runner_marker=/tmp/long-runner-init.txt.
scenarios:
  create:
    apply_responses:
      - task_name: "Write init marker on every host"
        run_result:
          status: success
          state_changes:
            files:
              - path: /tmp/long-runner-init.txt
                state: present
