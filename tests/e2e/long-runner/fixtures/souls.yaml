# Souls-фикстура для L3a long-runner::create.
#
# scenario create — простой write маркер; сами «длинные прогоны» (staggered,
# serial_waves) — отдельные сценарии, проверяются отдельным slice-ом. Здесь
# минимальный happy-path init.
- sid: soul-a.example.com
  status: connected
  covens: [runner]
  soulprint:
    os:
      family: debian
      distro: debian
      version: "12"
      pkg_mgr: apt
      init_system: systemd
    hostname: soul-a
