# L3a E2E fixture — Oracle typed-PortentPayload full-loop (V5-1, ADR-030).
#
# A single Soul-stub bound to the web-coven. After opening the EventStream, the stub
# emits a PortentEvent with typed file_changed → Oracle matches a Decree → queues
# the named-scenario in the work-queue → apply_runs(planned).
souls:
  - sid: soul-oracle-0.example.com
    coven:
      - web
      - test-oracle-typed
    transport: agent
    status: connected
