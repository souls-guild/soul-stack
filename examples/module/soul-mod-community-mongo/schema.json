{"kind":"soul_module","modules":[{"capabilities":["network_outbound"],"name":"mongo","side_effects":[{"service":"mongod"}],"states":{"command":{"description":"Run a raw command against MongoDB via db.runCommand (imperative verb-state).\nDefaults to changed=false (probe); the operator is responsible for idempotency.\nOutput.ok — success flag of the response. No dry-run preview.","input":{"addr":{"description":"mongod address: host:port (usually 127.0.0.1:27017).","required":true,"type":"string"},"auth_db":{"description":"authenticationDatabase (default admin).","type":"string"},"changed":{"default":false,"description":"Mark the result as changed=true (for commands that actually mutate state). Defaults to false (probe semantics).","type":"bool"},"command":{"description":"bson document of the command (first/only key — the command name), e.g. { serverStatus: 1 } or { collStats: \"events\" }. For PILOT — single-field commands.","required":true,"type":"map"},"db":{"description":"Target DB for runCommand (default admin).","type":"string"},"password":{"description":"MongoDB password (vault ref; keeper resolves before Apply). Masked — is not passed into the command arguments (goes into the connection).","pattern":"^vault:.*","secret":true,"type":"string"},"tls":{"default":false,"description":"Connect over TLS. Defaults to false. PILOT: mongo in plain — not set.","type":"bool"},"tls_ca":{"description":"CA PEM for verifying the server certificate. Masked (secret).","pattern":"^vault:.*","secret":true,"type":"string"},"tls_cert":{"description":"Client-cert PEM for mTLS (optional, with tls_key). Masked (secret).","pattern":"^vault:.*","secret":true,"type":"string"},"tls_key":{"description":"Client-key PEM for mTLS (optional, with tls_cert). Masked (secret).","pattern":"^vault:.*","secret":true,"type":"string"},"tls_skip_verify":{"default":false,"description":"EXPLICIT opt-out of certificate verification. Defaults to false (default secure).","type":"bool"},"username":{"description":"ACL username for AUTH.","type":"string"}}},"pinged":{"description":"MongoDB health probe via go-mongo-driver Ping (primary). Read-only,\nchanged=false by design (probe, not a mutation). Output.ok — condition for the\nhealth gate (until: register.self.ok == true). No dry-run preview.","input":{"addr":{"description":"mongod address: host:port (usually 127.0.0.1:27017).","required":true,"type":"string"},"auth_db":{"description":"authenticationDatabase (default admin).","type":"string"},"password":{"description":"MongoDB password (vault ref in operator-input; keeper resolves before Apply). Masked — does not end up in events/logs. Not passed to Ping (goes into the connection).","pattern":"^vault:.*","secret":true,"type":"string"},"tls":{"default":false,"description":"Connect to mongod over TLS. Defaults to false (plaintext). PILOT: mongo in plain mode — not set.","type":"bool"},"tls_ca":{"description":"PEM of the CA certificate for verifying the server certificate (RootCAs). Masked (secret). Resolved keeper-side from Vault (render phase).","pattern":"^vault:.*","secret":true,"type":"string"},"tls_cert":{"description":"PEM of the client certificate for mTLS (optional, only together with tls_key). Masked (secret).","pattern":"^vault:.*","secret":true,"type":"string"},"tls_key":{"description":"PEM of the client key for mTLS (optional, only together with tls_cert). Masked (secret); does not end up in events/errors.","pattern":"^vault:.*","secret":true,"type":"string"},"tls_skip_verify":{"default":false,"description":"EXPLICIT opt-out of server certificate verification. Defaults to false (verification enabled — default secure).","type":"bool"},"username":{"description":"ACL username for AUTH (if not anonymous).","type":"string"}}},"user":{"description":"Manage a MongoDB user (createUser/dropUser upsert) via go-mongo-driver.\nstate present — create the user (if missing), absent — remove it (if present).\nIdempotent via usersInfo (already in the desired state → changed=false). ★ the first\nadmin is created via the localhost-exception (no-auth loopback connection, while the\nadmin DB is empty). Changing the password/roles of an existing user — day-2 (out of PILOT scope).\nNo dry-run preview.","input":{"addr":{"description":"mongod address: host:port (usually 127.0.0.1:27017).","required":true,"type":"string"},"auth_db":{"description":"authenticationDatabase of the connection (default admin).","type":"string"},"database":{"description":"The DB in which the user is created (login/roles context). Default admin (system users). Roles without an explicit db inherit this DB.","type":"string"},"name":{"description":"MongoDB username (createUser/dropUser).","required":true,"type":"string"},"password":{"description":"Password of the ADMIN CONNECTION (the user is created under username; vault ref, keeper resolves before Apply). Goes into the connection credentials; does not end up in events/logs. ★ This is NOT the password of the user being created (see user_password) — except for bootstrapping the first admin, where the admin creates ITSELF (password serves both roles when user_password is not set).","pattern":"^vault:.*","secret":true,"type":"string"},"roles":{"description":"User roles (array of {role, db}) for state=present. Each element — {role: \"readWrite\", db: \"appdb\"}. db with no value inherits database. Required to be non-empty when present (a user without roles is meaningless).","type":"list"},"state":{"default":"present","description":"present (create if missing) / absent (remove if present). Default present.","type":"string"},"tls":{"default":false,"description":"Connect over TLS. Defaults to false. PILOT: mongo in plain — not set.","type":"bool"},"tls_ca":{"description":"CA PEM for verifying the server certificate. Masked (secret).","pattern":"^vault:.*","secret":true,"type":"string"},"tls_cert":{"description":"Client-cert PEM for mTLS (optional, with tls_key). Masked (secret).","pattern":"^vault:.*","secret":true,"type":"string"},"tls_key":{"description":"Client-key PEM for mTLS (optional, with tls_cert). Masked (secret).","pattern":"^vault:.*","secret":true,"type":"string"},"tls_skip_verify":{"default":false,"description":"EXPLICIT opt-out of certificate verification. Defaults to false (default secure).","type":"bool"},"user_password":{"description":"Password of the CREATED user (pwd of the createUser document; vault ref, keeper resolves before Apply). Separate from password (admin connection auth): for operator users the connection is under the admin password, while createUser uses this password. Not set → falls back to password (bootstrap of the first admin: the admin creates itself under the same password). Masked.","pattern":"^vault:.*","secret":true,"type":"string"},"username":{"description":"ACL username for the AUTH connection (the admin under which the user is created). During bootstrap of the first admin, auth is not yet possible → localhost-exception no-auth.","type":"string"}}}}}],"protocol_version":1}