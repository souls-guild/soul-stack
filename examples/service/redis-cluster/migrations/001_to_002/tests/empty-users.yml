name: empty-users
description: >
  Edge case: пустой список пользователей. foreach по пустой коллекции —
  no-op; map в state_after — пустой.

state_before:
  redis_users: []
  redis_maxmemory: "256mb"

state_after:
  redis_users: {}
  redis_maxmemory: "256mb"
