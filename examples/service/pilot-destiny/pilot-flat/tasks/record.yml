# within-destiny include neighbor (destiny/tasks.md §4): included from main.yml.
# Same structure — top-level list of tasks, sees only input.* of destiny.

- name: Record the marker placement
  module: core.exec.run
  changed_when: "false"
  params:
    cmd: echo
    args: ["marker", "written", "to", "${ input.marker_file }"]
