# L0-испытание create — persistence: aof_always. Пресет essence.persistence_presets
# [aof_always] = {save:"", appendonly:"yes", appendfsync:"always"} мержится в redis_config
# (fsync на КАЖДУЮ запись — максимум durability). Кейс держит инвариант «aof_always →
# appendfsync:always + appendonly:yes + save:"" в redis.conf И state». Контр-точка —
# rdb-кейсы (appendfsync отсутствует, appendonly:no). Парный rdb_aof_always несёт save.
# Режим sentinel single-host (1 master + 0 реплик).
name: persistence aof_always merges appendfsync always into redis config and state

fixtures:
  default_destiny_source: "file://../../destiny/{name}"
  hosts:
    - sid: node-solo.example.com
      covens: [create, redis]
      soulprint:
        network: { primary_ip: 10.0.0.5 }
        os: { arch: amd64 }
  vault:
    secret/redis/create:
      password: s3cr3t-redis-pass-create
    secret/redis/create/users/replica:
      password: replica-sys-secret-pass
    secret/redis/create/users/monitoring:
      password: monitoring-sys-secret-pass
    secret/redis/create/users/sentinel:
      password: sentinel-sys-secret-pass
    secret/redis/create/users/haproxy:
      password: haproxy-sys-secret-pass
  essence:
    redis_type: sentinel
    sentinel_master_name: master
    sentinel_master_defaults:
      down-after-milliseconds: "5000"
      failover-timeout:        "60000"
    modules_base_url: "https://artifacts.example.com/redis/modules"
    memory_reserve_percent: 75
    persistence_presets:
      aof_always:
        save:        ""
        appendonly:  "yes"
        appendfsync: "always"
    redis_config:
      maxmemory: "256mb"
    system_acl_users:
      replica:    "+psync +replconf +ping"
      monitoring: "-@all +@connection +client +ping +info +config|get +cluster|info +slowlog +latency +memory +select +command|count +command|docs"
      sentinel:   "allchannels +multi +slaveof +ping +exec +subscribe +config|rewrite +role +publish +info +client|setname +client|kill +script|kill"
      haproxy:    "-@all +auth +client|getname +client|id +client|setname +command +hello +ping +role +info +cluster|info"
    system_acl_users_sentinel:
      default:    "allchannels allkeys +@all"
      monitoring: "-@all +@connection -command +client -hello +info -auth +sentinel|masters +sentinel|replicas +sentinel|slaves +sentinel|sentinels +sentinel|ckquorum"
      sentinel:   "allchannels -@all +auth +client|getname +client|id +client|setname +command +hello +ping +sentinel|is-master-down-by-addr +publish +role +sentinel|get-master-addr-by-name +sentinel|master +sentinel|myid +sentinel|replicas +sentinel|sentinels"
      haproxy:    "-@all +auth +client|getname +client|id +client|setname +command +hello +ping +role +sentinel|get-master-addr-by-name +sentinel|master +sentinel|myid +sentinel|replicas +sentinel|sentinels"
  input:
    redis_type: sentinel
    version: "7.4.1"
    install_method: package
    replicas_per_master: 0
    # aof_always: пресет доливает appendfsync:always (fsync на каждую запись).
    persistence: aof_always

assert:
  task_present:
    - module: core.file.rendered
      params_subset:
        path: /etc/redis/redis.conf

  state_changes:
    redis_type: sentinel
    redis_version: "7.4.1"
    # Пресет aof_always влит ЦЕЛИКОМ: appendonly:yes, save:"" (AOF без RDB-снапшотов),
    # appendfsync:always (главный маркер режима).
    redis_config:
      maxmemory: "256mb"
      save: ""
      appendonly: "yes"
      appendfsync: "always"
    redis_users: []
    redis_hosts: []
    redis_sentinel:
      master_name: master
      quorum: 0
      master_settings:
        down-after-milliseconds: "5000"
        failover-timeout: "60000"
      settings: {}
