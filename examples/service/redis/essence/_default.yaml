# Baseline-essence сервиса redis (подложка автора, концепция defaults роли).
# Оператор переопределяет в spec инкарнации; на L0-прогоне ключевые значения
# дублируются в fixtures кейса.
#
# Это СЛОЙ ТРАНСЛЯЦИИ простого ввода оператора (architect 2026-06-22): данные-
# таблицы (persistence_presets, memory_reserve_percent) и merge-подложка
# redis_config живут здесь, scenario сливает их с вводом через merge() (templating
# §2.3). НИЧЕГО из этой трансляции не уходит в плагин — она нужна только render-у
# redis.conf at-create (источник истины).

# Режим развёртывания по умолчанию. Режимы сервиса — sentinel/cluster (2026-06-25);
# sentinel с replicas_per_master=0 даёт standalone-эквивалент.
redis_type: sentinel

# Логическое имя monitored master в Sentinel (директива sentinel.conf SENTINEL
# MONITOR + community.redis.sentinel master_name). Переехало из operator-input в
# essence (2026-06-25): author-context, оператор переопределяет в spec.essence.
# Дефолт `master` (раньше operator-default был `mymaster`).
sentinel_master_name: master

# Базовый URL источника .so Redis-модулей (без арх-сегмента). destiny `redis` строит
# полный URL per-host как <base_url>/<arch>/<имя.so> (Redis < 8: модули разворачиваются
# всегда все). Переехало из operator-input в essence (2026-06-25): author-context,
# оператор переопределяет в spec.essence под свой mirror. Дефолт "" — не задан (Redis
# 8+ — модули встроены, источник не нужен; на Redis < 8 оператор обязан переопределить).
modules_base_url: ""

# ── Способ установки binary: источник бинарей Redis из Nexus (редизайн) ──────────
# install_method=binary (DEFAULT) тянет ОТДЕЛЬНЫЕ бинари redis-server/redis-cli/
# redis-benchmark из Nexus. base_url — author-context (оператор переопределяет в
# spec.essence под свой mirror). destiny строит путь per-host как
# <binary_base_url>/<arch>/Debian/<distro_ver>/<version>/redis-{server,cli,benchmark}
# из soulprint.self.os (arch, version) + input.version (cluster.yml/sentinel.yml binary-ветка).
# ★ binary_version в СЦЕНАРИИ create НЕ читается (версия — операторский input.version,
# enum выкаченных в Nexus сборок). НО он ЖИВ для других сценариев сервиса: migrate_cluster
# (df_version-каскад binary → essence.binary_version) — поэтому НЕ удалён, оставлен "" как
# author-context-подложка (оператор переопределяет в spec.essence под binary-инсталляцию).
binary_base_url: "https://nexus-hosted.wb.ru/repository/raw/devops/dba/prod/redis"
binary_version:  ""

# ── Дефолты per-master директив Sentinel (подложка sentinel_master_settings) ─────
# Нижний слой merge() для input.sentinel_master_settings (compute.sentinel_directives):
# ключ — имя опции SENTINEL SET без префикса/master-name, значение — строка. Сюда
# переехали дефолты удалённых именованных полей sentinel_down_after_ms (=5000) и
# sentinel_failover_timeout_ms (=60000). Оператор перекрывает их в Run-форме
# (input.sentinel_master_settings, last-wins) или в spec.essence.
sentinel_master_defaults:
  down-after-milliseconds: "5000"
  failover-timeout:        "60000"

# ── Host-layout: базовые каталоги конфигурации и данных Redis (#2, 2026-06-26) ───
# conf_dir/data_dir переехали из operator-input в essence (author-context): оператор
# переопределяет раскладку в spec.essence под свой storage-layout, а не в Run-форме
# каждого прогона. compute читает их отсюда и гонит в destiny (apply.input) + персистит
# в state (day-2 add_node читает layout из incarnation.state). Дефолт = текущий путь.
conf_dir: /etc/redis
data_dir: /var/lib/redis

# Доля бюджета памяти (input.memory_mb), отдаваемая Redis под maxmemory; остаток —
# резерв под ОС/накладные. scenario вычисляет maxmemory = memory_mb*pct/100 (МБ).
memory_reserve_percent: 75

# Данные-таблица persistence: enum оператора (off/aof_1sec/aof_always/rdb/
# rdb_aof_1sec/rdb_aof_always) → директивы redis.conf. scenario берёт
# presets[input.persistence] и мержит ЦЕЛИКОМ в redis_config (appendfsync доезжает
# автоматически). Значения — строки (range-ятся в redis.conf как `<key> <value>`):
#   - save: "" отключает RDB-снапшоты; непустая — politica save точек.
#   - appendonly: yes/no включает AOF.
#   - appendfsync: everysec/always — частота fsync AOF (для AOF-режимов).
# rdb_aof_* = и снапшоты, и AOF (двойная durability). aof_1sec/rdb_aof_1sec — fsync
# раз в секунду (баланс durability/throughput); aof_always/rdb_aof_always — fsync на
# каждую запись (максимальная durability, дороже).
persistence_presets:
  off:
    save:       ""
    appendonly: "no"
  aof_1sec:
    save:        ""
    appendonly:  "yes"
    appendfsync: "everysec"
  aof_always:
    save:        ""
    appendonly:  "yes"
    appendfsync: "always"
  rdb:
    save:       "900 1 300 10 60 10000"
    appendonly: "no"
  rdb_aof_1sec:
    save:        "900 1 300 10 60 10000"
    appendonly:  "yes"
    appendfsync: "everysec"
  rdb_aof_always:
    save:        "900 1 300 10 60 10000"
    appendonly:  "yes"
    appendfsync: "always"

# TLS (концепция Ansible-роли redis: defaults redis_tls_*). Источник PEM —
# Vault-ПУТИ (не сам PEM): scenario резолвит их через ${ vault(<ref>) } в render-
# фазе и передаёт ЦЕЛЫЕ PEM в destiny/плагин. Пути — доверенный контекст автора
# (essence), не operator-input → vault() принимает их динамическим аргументом
# (templating.md §2.3). Форма ref — "<mount>/<path>#<field>" (как requirepass).
# ★ Редизайн (connection_mode): TLS-НАМЕРЕНИЕ переехало в operator-input
# input.connection_mode (enum tls/tls_plain/plain) — прежние essence-флаги
# tls_enable/tls_only УДАЛЕНЫ (compute.tls_on/tls_only_on выводятся из enum, не из
# essence). В essence остаются лишь ТЕХНИЧЕСКИЕ параметры: tls_port (7379, как роль)
# и Vault-пути PEM ниже.
tls_port:   7379
# Vault-пути источника PEM (заполнить под реальную инсталляцию). Дефолт —
# конвенция secret/services/redis/tls (симметрия с secret/redis/<inc> паролей).
tls_cert_ref: "secret/services/redis/tls#cert"
tls_key_ref:  "secret/services/redis/tls#key"
tls_ca_ref:   "secret/services/redis/tls#ca"

# ── Системные ACL-юзеры Redis (концепция Ansible-роли redis: redis_system_users /
#    redis_sentinel_system_users) ────────────────────────────────────────────────
# Имя→ACL-perms-строка СЛУЖЕБНЫХ юзеров, без которых кластер не работает: replica
# (репликация PSYNC), monitoring (экспортёр метрик), sentinel (sentinel↔redis AUTH),
# haproxy (health-check балансировщика). perms скопированы 1:1 из vars/main.yml роли
# (redis_system_users / redis_sentinel_system_users) — НЕ выдумывать, источник истины.
#
# Два набора, потому что у redis-server и sentinel-демона РАЗНЫЕ требования к командам
# (sentinel-команды vs обычные redis-команды) → ДВА aclfile: users.acl (redis-server,
# system_acl_users) и sentinel-users.acl (sentinel-демон, system_acl_users_sentinel).
#
# Пароль КАЖДОГО системного юзера — в Vault по той же ветке, что operator-extra:
# secret/redis/<incarnation>/users/<name>#password. scenario резолвит keeper-side
# через vault() в render-фазе и доливает системных в apply.input.users ПОВЕРХ
# operator-extra (last-wins; см. create/cluster.yml/sentinel.yml, add_user).
#
# ★ default-юзер: в system_acl_users (redis-server) его НЕТ — он задаётся requirepass
# в redis.conf (модель кирпича: redis.conf.tmpl «requirepass {{.vars.password}}»,
# users.acl.tmpl его не дублирует). В system_acl_users_sentinel default ЕСТЬ — у
# sentinel-демона нет requirepass-эквивалента для aclfile-доступа, поэтому default
# объявляется в sentinel-users.acl с тем же секретом secret/redis/<inc>#password
# (как роль). Имена системных = default/replica/monitoring/sentinel/haproxy —
# validate-guard create отвергает operator-extra с любым из этих имён (коллизия).
#
# Набор redis-server (redis_system_users роли, БЕЗ default — он в requirepass):
system_acl_users:
  replica:    "+psync +replconf +ping"
  monitoring: "-@all +@connection +client +ping +info +config|get +cluster|info +slowlog +latency +memory +select +command|count +command|docs"
  sentinel:   "allchannels +multi +slaveof +ping +exec +subscribe +config|rewrite +role +publish +info +client|setname +client|kill +script|kill"
  haproxy:    "-@all +auth +client|getname +client|id +client|setname +command +hello +ping +role +info +cluster|info"

# Набор sentinel-демона (redis_sentinel_system_users роли; ВКЛЮЧАЕТ default):
system_acl_users_sentinel:
  default:    "allchannels allkeys +@all"
  monitoring: "-@all +@connection -command +client -hello +info -auth +sentinel|masters +sentinel|replicas +sentinel|slaves +sentinel|sentinels +sentinel|ckquorum"
  sentinel:   "allchannels -@all +auth +client|getname +client|id +client|setname +command +hello +ping +sentinel|is-master-down-by-addr +publish +role +sentinel|get-master-addr-by-name +sentinel|master +sentinel|myid +sentinel|replicas +sentinel|sentinels"
  haproxy:    "-@all +auth +client|getname +client|id +client|setname +command +hello +ping +role +sentinel|get-master-addr-by-name +sentinel|master +sentinel|myid +sentinel|replicas +sentinel|sentinels"

# Merge-подложка redis.conf (defaults роли) — НИЖНИЙ слой merge(): persistence-
# пресет, вычисленные maxmemory/policy и passthrough оператора наслаиваются
# поверх. Этот merged итог рендерится в полный redis.conf (destiny `redis`).
redis_config:
  maxmemory:        "256mb"
  maxmemory-policy: allkeys-lru
  maxclients:       10000
  timeout:          300

# Данные-таблица kernel-параметров под Redis. scenario передаёт этот map в destiny
# `redis` через apply.input.sysctl_settings → core.sysctl.applied строит drop-in
# /etc/sysctl.d/30-redis.conf и применяет его `sysctl -e -p <file>` БЕЗУСЛОВНО
# (host-tuning — рекомендация Redis, не выбор оператора; операторского флага
# sysctl_enable нет). Значения — СТРОКИ (multi-value параметры через пробел); модуль
# сортирует ключи при построении drop-in (детерминизм → нет ложного reload). Не во
# входе оператора: tuning под Redis, не операционный выбор.
#
# ИСТОЧНИК (выровнено 2026-06-23): набор и значения 1:1 повторяют sysctl-блок
# Ansible-роли redis (roles/redis/tasks/installation.yml «Настройка параметров
# ядра»), который ссылается на Redis admin-docs (background-save fork-error +
# kernel-network-stack tuning). БЕЗ блока tcp_bbr (net.core.default_qdisc=fq +
# net.ipv4.tcp_congestion_control=bbr) — он зависит от загрузки модуля ядра tcp_bbr
# (на Debian не загружен по умолчанию) → отложен отдельным шагом загрузки модуля.
sysctl_settings:
  # >>> Память и процессы (КРИТИЧНО для fork RDB/AOF и больших инстансов) <<<
  # overcommit_memory=1 — Redis-рекомендация: иначе background save падает с
  # fork-error (redis-docs FAQ background-saving-fails). swappiness=1 — исключаем
  # вытеснение в swap кроме крайней ситуации.
  vm.overcommit_memory:  "1"
  vm.swappiness:         "1"
  vm.max_map_count:      "262144"
  vm.vfs_cache_pressure: "200"
  # >>> Сетевые буферы (производительность под нагрузкой, Redis kernel-stack tuning) <<<
  net.core.rmem_max:     "134217728"
  net.core.wmem_max:     "134217728"
  net.core.rmem_default: "212992"
  net.core.wmem_default: "212992"
  net.ipv4.tcp_rmem:     "4096 212992 134217728"
  net.ipv4.tcp_wmem:     "4096 212992 134217728"
  # >>> Очереди и бэклоги <<<
  net.core.somaxconn:           "65535"
  net.core.netdev_max_backlog:  "20000"
  net.ipv4.tcp_max_syn_backlog: "65535"
  # >>> TCP-стек (Redis kernel-network-stack tuning) <<<
  net.ipv4.tcp_sack:           "1"
  net.ipv4.tcp_timestamps:     "1"
  net.ipv4.tcp_window_scaling: "1"
  net.ipv4.tcp_syncookies:     "1"
  net.ipv4.tcp_tw_reuse:       "1"
  net.ipv4.tcp_fin_timeout:    "30"
  net.ipv4.ip_local_port_range: "30000 65535"

# ── Каталог валидных директив redis.conf по версиям (version-aware validation, #6) ──
# ★ #6 (2026-06-26): version-aware валидатор имён директив для input.redis_settings.
# redis_settings — passthrough KEY→VALUE: без проверки оператор молча протащит опечатку
# (`maxmemoyr 512mb`) или директиву из чужой версии → redis-server не стартует на хосте.
# Каталог = плоское множество ВАЛИДНЫХ ИМЁН директив для серии Redis (major.minor).
# scenario/create assert-ит, что КАЖДЫЙ ключ redis_settings ∈ каталог[серия input.version]
# → неизвестное имя рвёт прогон на render (422 assert-failed) ДО applying, понятным
# сообщением. MVP: только ИМЕНА (значения не валидируются).
#
# Ключ — серия major.minor (директивы стабильны внутри патч-серии). input.version —
# distro-пин (5:7.0.15-1~deb12u7); assert извлекает X.Y регуляркой. Серии нет в каталоге
# → assert skip (валидируем только известные версии; не-каталожная версия не блокируется).
#
# ИСТОЧНИК — src/config.c апстрима Redis (НЕ redis.conf: прозаические комментарии
# неотличимы от commented-примеров директив). Генерируется scripts/gen-redis-catalog.sh
# (curl config.c + extract имён макросов createXConfig + alias + специальные директивы),
# результат COMMITTED здесь — на render-пути сети нет. Перегенерировать при добавлении
# версии: `scripts/gen-redis-catalog.sh > /tmp/x && вклеить redis_directives:` сюда.
redis_directives:
  "6.2":
    - acl-pubsub-default
    - aclfile
    - acllog-max-len
    - active-defrag-cycle-max
    - active-defrag-cycle-min
    - active-defrag-ignore-bytes
    - active-defrag-max-scan-fields
    - active-defrag-threshold-lower
    - active-defrag-threshold-upper
    - active-expire-effort
    - activedefrag
    - activerehashing
    - always-show-logo
    - aof-load-truncated
    - aof-rewrite-incremental-fsync
    - aof-use-rdb-preamble
    - aof_rewrite_cpulist
    - appendfilename
    - appendfsync
    - appendonly
    - auto-aof-rewrite-min-size
    - auto-aof-rewrite-percentage
    - bgsave_cpulist
    - bio_cpulist
    - client-query-buffer-limit
    - cluster-allow-reads-when-down
    - cluster-allow-replica-migration
    - cluster-announce-bus-port
    - cluster-announce-ip
    - cluster-announce-port
    - cluster-announce-tls-port
    - cluster-enabled
    - cluster-migration-barrier
    - cluster-node-timeout
    - cluster-replica-no-failover
    - cluster-replica-validity-factor
    - cluster-require-full-coverage
    - cluster-slave-no-failover
    - cluster-slave-validity-factor
    - crash-log-enabled
    - crash-memcheck-enabled
    - daemonize
    - databases
    - dbfilename
    - disable-thp
    - dynamic-hz
    - gopher-enabled
    - hash-max-ziplist-entries
    - hash-max-ziplist-value
    - hll-sparse-max-bytes
    - hz
    - ignore-warnings
    - include
    - io-threads
    - io-threads-do-reads
    - jemalloc-bg-thread
    - key-load-delay
    - latency-monitor-threshold
    - lazyfree-lazy-eviction
    - lazyfree-lazy-expire
    - lazyfree-lazy-server-del
    - lazyfree-lazy-user-del
    - lazyfree-lazy-user-flush
    - lfu-decay-time
    - lfu-log-factor
    - list-compress-depth
    - list-max-ziplist-size
    - loadmodule
    - loglevel
    - lua-enable-deprecated-api
    - lua-replicate-commands
    - lua-time-limit
    - masterauth
    - masteruser
    - maxclients
    - maxmemory
    - maxmemory-eviction-tenacity
    - maxmemory-policy
    - maxmemory-samples
    - min-replicas-max-lag
    - min-replicas-to-write
    - min-slaves-max-lag
    - min-slaves-to-write
    - no-appendfsync-on-rewrite
    - oom-score-adj
    - pidfile
    - port
    - proc-title-template
    - protected-mode
    - proto-max-bulk-len
    - rdb-del-sync-files
    - rdb-key-save-delay
    - rdb-save-incremental-fsync
    - rdbchecksum
    - rdbcompression
    - rename-command
    - repl-backlog-size
    - repl-backlog-ttl
    - repl-disable-tcp-nodelay
    - repl-diskless-load
    - repl-diskless-sync
    - repl-diskless-sync-delay
    - repl-ping-replica-period
    - repl-ping-slave-period
    - repl-timeout
    - replica-announce-ip
    - replica-announce-port
    - replica-announced
    - replica-ignore-maxmemory
    - replica-lazy-flush
    - replica-priority
    - replica-read-only
    - replica-serve-stale-data
    - requirepass
    - sanitize-dump-payload
    - sentinel
    - server_cpulist
    - set-max-intset-entries
    - set-proc-title
    - slave-announce-ip
    - slave-announce-port
    - slave-ignore-maxmemory
    - slave-lazy-flush
    - slave-priority
    - slave-read-only
    - slave-serve-stale-data
    - slaveof
    - slowlog-log-slower-than
    - slowlog-max-len
    - stop-writes-on-bgsave-error
    - stream-node-max-bytes
    - stream-node-max-entries
    - supervised
    - syslog-enabled
    - syslog-facility
    - syslog-ident
    - tcp-backlog
    - tcp-keepalive
    - timeout
    - tls-auth-clients
    - tls-ca-cert-dir
    - tls-ca-cert-file
    - tls-cert-file
    - tls-ciphers
    - tls-ciphersuites
    - tls-client-cert-file
    - tls-client-key-file
    - tls-client-key-file-pass
    - tls-cluster
    - tls-dh-params-file
    - tls-key-file
    - tls-key-file-pass
    - tls-port
    - tls-prefer-server-ciphers
    - tls-protocols
    - tls-replication
    - tls-session-cache-size
    - tls-session-cache-timeout
    - tls-session-caching
    - tracking-table-max-keys
    - unixsocket
    - use-exit-on-panic
    - user
    - zset-max-ziplist-entries
    - zset-max-ziplist-value
  "7.0":
    - acl-pubsub-default
    - aclfile
    - acllog-max-len
    - active-defrag-cycle-max
    - active-defrag-cycle-min
    - active-defrag-ignore-bytes
    - active-defrag-max-scan-fields
    - active-defrag-threshold-lower
    - active-defrag-threshold-upper
    - active-expire-effort
    - activedefrag
    - activerehashing
    - always-show-logo
    - aof-disable-auto-gc
    - aof-load-truncated
    - aof-rewrite-incremental-fsync
    - aof-timestamp-enabled
    - aof-use-rdb-preamble
    - aof_rewrite_cpulist
    - appenddirname
    - appendfilename
    - appendfsync
    - appendonly
    - auto-aof-rewrite-min-size
    - auto-aof-rewrite-percentage
    - bgsave_cpulist
    - bind
    - bind-source-addr
    - bio_cpulist
    - busy-reply-threshold
    - client-output-buffer-limit
    - client-query-buffer-limit
    - cluster-allow-pubsubshard-when-down
    - cluster-allow-reads-when-down
    - cluster-allow-replica-migration
    - cluster-announce-bus-port
    - cluster-announce-hostname
    - cluster-announce-ip
    - cluster-announce-port
    - cluster-announce-tls-port
    - cluster-config-file
    - cluster-enabled
    - cluster-link-sendbuf-limit
    - cluster-migration-barrier
    - cluster-node-timeout
    - cluster-port
    - cluster-preferred-endpoint-type
    - cluster-replica-no-failover
    - cluster-replica-validity-factor
    - cluster-require-full-coverage
    - cluster-slave-no-failover
    - cluster-slave-validity-factor
    - crash-log-enabled
    - crash-memcheck-enabled
    - daemonize
    - databases
    - dbfilename
    - dir
    - disable-thp
    - dynamic-hz
    - enable-debug-command
    - enable-module-command
    - enable-protected-configs
    - hash-max-listpack-entries
    - hash-max-listpack-value
    - hash-max-ziplist-entries
    - hash-max-ziplist-value
    - hll-sparse-max-bytes
    - hz
    - ignore-warnings
    - include
    - io-threads
    - io-threads-do-reads
    - jemalloc-bg-thread
    - key-load-delay
    - latency-monitor-threshold
    - latency-tracking
    - latency-tracking-info-percentiles
    - lazyfree-lazy-eviction
    - lazyfree-lazy-expire
    - lazyfree-lazy-server-del
    - lazyfree-lazy-user-del
    - lazyfree-lazy-user-flush
    - lfu-decay-time
    - lfu-log-factor
    - list-compress-depth
    - list-max-listpack-size
    - list-max-ziplist-size
    - loading-process-events-interval-bytes
    - loadmodule
    - logfile
    - loglevel
    - lua-time-limit
    - masterauth
    - masteruser
    - maxclients
    - maxmemory
    - maxmemory-clients
    - maxmemory-eviction-tenacity
    - maxmemory-policy
    - maxmemory-samples
    - min-replicas-max-lag
    - min-replicas-to-write
    - min-slaves-max-lag
    - min-slaves-to-write
    - no-appendfsync-on-rewrite
    - notify-keyspace-events
    - oom-score-adj
    - oom-score-adj-values
    - pidfile
    - port
    - proc-title-template
    - propagation-error-behavior
    - protected-mode
    - proto-max-bulk-len
    - rdb-del-sync-files
    - rdb-key-save-delay
    - rdb-save-incremental-fsync
    - rdbchecksum
    - rdbcompression
    - rename-command
    - repl-backlog-size
    - repl-backlog-ttl
    - repl-disable-tcp-nodelay
    - repl-diskless-load
    - repl-diskless-sync
    - repl-diskless-sync-delay
    - repl-diskless-sync-max-replicas
    - repl-ping-replica-period
    - repl-ping-slave-period
    - repl-timeout
    - replica-announce-ip
    - replica-announce-port
    - replica-announced
    - replica-ignore-disk-write-errors
    - replica-ignore-maxmemory
    - replica-lazy-flush
    - replica-priority
    - replica-read-only
    - replica-serve-stale-data
    - replicaof
    - requirepass
    - sanitize-dump-payload
    - save
    - sentinel
    - server_cpulist
    - set-max-intset-entries
    - set-proc-title
    - shutdown-on-sigint
    - shutdown-on-sigterm
    - shutdown-timeout
    - slave-announce-ip
    - slave-announce-port
    - slave-ignore-maxmemory
    - slave-lazy-flush
    - slave-priority
    - slave-read-only
    - slave-serve-stale-data
    - slaveof
    - slowlog-log-slower-than
    - slowlog-max-len
    - socket-mark-id
    - stop-writes-on-bgsave-error
    - stream-node-max-bytes
    - stream-node-max-entries
    - supervised
    - syslog-enabled
    - syslog-facility
    - syslog-ident
    - tcp-backlog
    - tcp-keepalive
    - timeout
    - tls-auth-clients
    - tls-ca-cert-dir
    - tls-ca-cert-file
    - tls-cert-file
    - tls-ciphers
    - tls-ciphersuites
    - tls-client-cert-file
    - tls-client-key-file
    - tls-client-key-file-pass
    - tls-cluster
    - tls-dh-params-file
    - tls-key-file
    - tls-key-file-pass
    - tls-port
    - tls-prefer-server-ciphers
    - tls-protocols
    - tls-replication
    - tls-session-cache-size
    - tls-session-cache-timeout
    - tls-session-caching
    - tracking-table-max-keys
    - unixsocket
    - unixsocketperm
    - use-exit-on-panic
    - user
    - watchdog-period
    - zset-max-listpack-entries
    - zset-max-listpack-value
    - zset-max-ziplist-entries
    - zset-max-ziplist-value
  "7.2":
    - acl-pubsub-default
    - aclfile
    - acllog-max-len
    - active-defrag-cycle-max
    - active-defrag-cycle-min
    - active-defrag-ignore-bytes
    - active-defrag-max-scan-fields
    - active-defrag-threshold-lower
    - active-defrag-threshold-upper
    - active-expire-effort
    - activedefrag
    - activerehashing
    - always-show-logo
    - aof-disable-auto-gc
    - aof-load-truncated
    - aof-rewrite-incremental-fsync
    - aof-timestamp-enabled
    - aof-use-rdb-preamble
    - aof_rewrite_cpulist
    - appenddirname
    - appendfilename
    - appendfsync
    - appendonly
    - auto-aof-rewrite-min-size
    - auto-aof-rewrite-percentage
    - bgsave_cpulist
    - bind
    - bind-source-addr
    - bio_cpulist
    - busy-reply-threshold
    - client-default-resp
    - client-output-buffer-limit
    - client-query-buffer-limit
    - cluster-allow-pubsubshard-when-down
    - cluster-allow-reads-when-down
    - cluster-allow-replica-migration
    - cluster-announce-bus-port
    - cluster-announce-hostname
    - cluster-announce-human-nodename
    - cluster-announce-ip
    - cluster-announce-port
    - cluster-announce-tls-port
    - cluster-config-file
    - cluster-enabled
    - cluster-link-sendbuf-limit
    - cluster-migration-barrier
    - cluster-node-timeout
    - cluster-ping-interval
    - cluster-port
    - cluster-preferred-endpoint-type
    - cluster-replica-no-failover
    - cluster-replica-validity-factor
    - cluster-require-full-coverage
    - cluster-slave-no-failover
    - cluster-slave-validity-factor
    - crash-log-enabled
    - crash-memcheck-enabled
    - daemonize
    - databases
    - dbfilename
    - dir
    - disable-thp
    - dynamic-hz
    - enable-debug-command
    - enable-module-command
    - enable-protected-configs
    - hash-max-listpack-entries
    - hash-max-listpack-value
    - hash-max-ziplist-entries
    - hash-max-ziplist-value
    - hll-sparse-max-bytes
    - hz
    - ignore-warnings
    - include
    - io-threads
    - io-threads-do-reads
    - jemalloc-bg-thread
    - key-load-delay
    - latency-monitor-threshold
    - latency-tracking
    - latency-tracking-info-percentiles
    - lazyfree-lazy-eviction
    - lazyfree-lazy-expire
    - lazyfree-lazy-server-del
    - lazyfree-lazy-user-del
    - lazyfree-lazy-user-flush
    - lfu-decay-time
    - lfu-log-factor
    - list-compress-depth
    - list-max-listpack-size
    - list-max-ziplist-size
    - loading-process-events-interval-bytes
    - loadmodule
    - locale-collate
    - logfile
    - loglevel
    - lua-enable-deprecated-api
    - lua-time-limit
    - masterauth
    - masteruser
    - maxclients
    - maxmemory
    - maxmemory-clients
    - maxmemory-eviction-tenacity
    - maxmemory-policy
    - maxmemory-samples
    - min-replicas-max-lag
    - min-replicas-to-write
    - min-slaves-max-lag
    - min-slaves-to-write
    - no-appendfsync-on-rewrite
    - notify-keyspace-events
    - oom-score-adj
    - oom-score-adj-values
    - pidfile
    - port
    - proc-title-template
    - propagation-error-behavior
    - protected-mode
    - proto-max-bulk-len
    - rdb-del-sync-files
    - rdb-key-save-delay
    - rdb-save-incremental-fsync
    - rdbchecksum
    - rdbcompression
    - rename-command
    - repl-backlog-size
    - repl-backlog-ttl
    - repl-disable-tcp-nodelay
    - repl-diskless-load
    - repl-diskless-sync
    - repl-diskless-sync-delay
    - repl-diskless-sync-max-replicas
    - repl-ping-replica-period
    - repl-ping-slave-period
    - repl-timeout
    - replica-announce-ip
    - replica-announce-port
    - replica-announced
    - replica-ignore-disk-write-errors
    - replica-ignore-maxmemory
    - replica-lazy-flush
    - replica-priority
    - replica-read-only
    - replica-serve-stale-data
    - replicaof
    - req-res-logfile
    - requirepass
    - sanitize-dump-payload
    - save
    - sentinel
    - server_cpulist
    - set-max-intset-entries
    - set-max-listpack-entries
    - set-max-listpack-value
    - set-proc-title
    - shutdown-on-sigint
    - shutdown-on-sigterm
    - shutdown-timeout
    - slave-announce-ip
    - slave-announce-port
    - slave-ignore-maxmemory
    - slave-lazy-flush
    - slave-priority
    - slave-read-only
    - slave-serve-stale-data
    - slaveof
    - slowlog-log-slower-than
    - slowlog-max-len
    - socket-mark-id
    - stop-writes-on-bgsave-error
    - stream-node-max-bytes
    - stream-node-max-entries
    - supervised
    - syslog-enabled
    - syslog-facility
    - syslog-ident
    - tcp-backlog
    - tcp-keepalive
    - timeout
    - tls-auth-clients
    - tls-ca-cert-dir
    - tls-ca-cert-file
    - tls-cert-file
    - tls-ciphers
    - tls-ciphersuites
    - tls-client-cert-file
    - tls-client-key-file
    - tls-client-key-file-pass
    - tls-cluster
    - tls-dh-params-file
    - tls-key-file
    - tls-key-file-pass
    - tls-port
    - tls-prefer-server-ciphers
    - tls-protocols
    - tls-replication
    - tls-session-cache-size
    - tls-session-cache-timeout
    - tls-session-caching
    - tracking-table-max-keys
    - unixsocket
    - unixsocketperm
    - use-exit-on-panic
    - user
    - watchdog-period
    - zset-max-listpack-entries
    - zset-max-listpack-value
    - zset-max-ziplist-entries
    - zset-max-ziplist-value
  "7.4":
    - acl-pubsub-default
    - aclfile
    - acllog-max-len
    - active-defrag-cycle-max
    - active-defrag-cycle-min
    - active-defrag-ignore-bytes
    - active-defrag-max-scan-fields
    - active-defrag-threshold-lower
    - active-defrag-threshold-upper
    - active-expire-effort
    - activedefrag
    - activerehashing
    - always-show-logo
    - aof-disable-auto-gc
    - aof-load-truncated
    - aof-rewrite-cpulist
    - aof-rewrite-incremental-fsync
    - aof-timestamp-enabled
    - aof-use-rdb-preamble
    - aof_rewrite_cpulist
    - appenddirname
    - appendfilename
    - appendfsync
    - appendonly
    - auto-aof-rewrite-min-size
    - auto-aof-rewrite-percentage
    - bgsave-cpulist
    - bgsave_cpulist
    - bind
    - bind-source-addr
    - bio-cpulist
    - bio_cpulist
    - busy-reply-threshold
    - client-default-resp
    - client-output-buffer-limit
    - client-query-buffer-limit
    - cluster-allow-pubsubshard-when-down
    - cluster-allow-reads-when-down
    - cluster-allow-replica-migration
    - cluster-announce-bus-port
    - cluster-announce-hostname
    - cluster-announce-human-nodename
    - cluster-announce-ip
    - cluster-announce-port
    - cluster-announce-tls-port
    - cluster-config-file
    - cluster-enabled
    - cluster-link-sendbuf-limit
    - cluster-migration-barrier
    - cluster-node-timeout
    - cluster-ping-interval
    - cluster-port
    - cluster-preferred-endpoint-type
    - cluster-replica-no-failover
    - cluster-replica-validity-factor
    - cluster-require-full-coverage
    - cluster-slave-no-failover
    - cluster-slave-validity-factor
    - crash-log-enabled
    - crash-memcheck-enabled
    - daemonize
    - databases
    - dbfilename
    - dir
    - disable-thp
    - dynamic-hz
    - enable-debug-command
    - enable-module-command
    - enable-protected-configs
    - hash-max-listpack-entries
    - hash-max-listpack-value
    - hash-max-ziplist-entries
    - hash-max-ziplist-value
    - hide-user-data-from-log
    - hll-sparse-max-bytes
    - hz
    - ignore-warnings
    - include
    - io-threads
    - io-threads-do-reads
    - jemalloc-bg-thread
    - key-load-delay
    - latency-monitor-threshold
    - latency-tracking
    - latency-tracking-info-percentiles
    - lazyfree-lazy-eviction
    - lazyfree-lazy-expire
    - lazyfree-lazy-server-del
    - lazyfree-lazy-user-del
    - lazyfree-lazy-user-flush
    - lfu-decay-time
    - lfu-log-factor
    - list-compress-depth
    - list-max-listpack-size
    - list-max-ziplist-size
    - loading-process-events-interval-bytes
    - loadmodule
    - locale-collate
    - logfile
    - loglevel
    - lua-enable-deprecated-api
    - lua-time-limit
    - masterauth
    - masteruser
    - max-new-connections-per-cycle
    - max-new-tls-connections-per-cycle
    - maxclients
    - maxmemory
    - maxmemory-clients
    - maxmemory-eviction-tenacity
    - maxmemory-policy
    - maxmemory-samples
    - min-replicas-max-lag
    - min-replicas-to-write
    - min-slaves-max-lag
    - min-slaves-to-write
    - no-appendfsync-on-rewrite
    - notify-keyspace-events
    - oom-score-adj
    - oom-score-adj-values
    - pidfile
    - port
    - proc-title-template
    - propagation-error-behavior
    - protected-mode
    - proto-max-bulk-len
    - rdb-del-sync-files
    - rdb-key-save-delay
    - rdb-save-incremental-fsync
    - rdbchecksum
    - rdbcompression
    - rename-command
    - repl-backlog-size
    - repl-backlog-ttl
    - repl-disable-tcp-nodelay
    - repl-diskless-load
    - repl-diskless-sync
    - repl-diskless-sync-delay
    - repl-diskless-sync-max-replicas
    - repl-ping-replica-period
    - repl-ping-slave-period
    - repl-timeout
    - replica-announce-ip
    - replica-announce-port
    - replica-announced
    - replica-ignore-disk-write-errors
    - replica-ignore-maxmemory
    - replica-lazy-flush
    - replica-priority
    - replica-read-only
    - replica-serve-stale-data
    - replicaof
    - req-res-logfile
    - requirepass
    - sanitize-dump-payload
    - save
    - sentinel
    - server-cpulist
    - server_cpulist
    - set-max-intset-entries
    - set-max-listpack-entries
    - set-max-listpack-value
    - set-proc-title
    - shutdown-on-sigint
    - shutdown-on-sigterm
    - shutdown-timeout
    - slave-announce-ip
    - slave-announce-port
    - slave-ignore-maxmemory
    - slave-lazy-flush
    - slave-priority
    - slave-read-only
    - slave-serve-stale-data
    - slaveof
    - slowlog-log-slower-than
    - slowlog-max-len
    - socket-mark-id
    - stop-writes-on-bgsave-error
    - stream-node-max-bytes
    - stream-node-max-entries
    - supervised
    - syslog-enabled
    - syslog-facility
    - syslog-ident
    - tcp-backlog
    - tcp-keepalive
    - timeout
    - tls-auth-clients
    - tls-ca-cert-dir
    - tls-ca-cert-file
    - tls-cert-file
    - tls-ciphers
    - tls-ciphersuites
    - tls-client-cert-file
    - tls-client-key-file
    - tls-client-key-file-pass
    - tls-cluster
    - tls-dh-params-file
    - tls-key-file
    - tls-key-file-pass
    - tls-port
    - tls-prefer-server-ciphers
    - tls-protocols
    - tls-replication
    - tls-session-cache-size
    - tls-session-cache-timeout
    - tls-session-caching
    - tracking-table-max-keys
    - unixsocket
    - unixsocketperm
    - use-exit-on-panic
    - user
    - watchdog-period
    - zset-max-listpack-entries
    - zset-max-listpack-value
    - zset-max-ziplist-entries
    - zset-max-ziplist-value
  "8.0":
    - acl-pubsub-default
    - aclfile
    - acllog-max-len
    - active-defrag-cycle-max
    - active-defrag-cycle-min
    - active-defrag-ignore-bytes
    - active-defrag-max-scan-fields
    - active-defrag-threshold-lower
    - active-defrag-threshold-upper
    - active-expire-effort
    - activedefrag
    - activerehashing
    - always-show-logo
    - aof-disable-auto-gc
    - aof-load-truncated
    - aof-rewrite-cpulist
    - aof-rewrite-incremental-fsync
    - aof-timestamp-enabled
    - aof-use-rdb-preamble
    - aof_rewrite_cpulist
    - appenddirname
    - appendfilename
    - appendfsync
    - appendonly
    - auto-aof-rewrite-min-size
    - auto-aof-rewrite-percentage
    - bgsave-cpulist
    - bgsave_cpulist
    - bind
    - bind-source-addr
    - bio-cpulist
    - bio_cpulist
    - busy-reply-threshold
    - client-default-resp
    - client-output-buffer-limit
    - client-query-buffer-limit
    - cluster-allow-pubsubshard-when-down
    - cluster-allow-reads-when-down
    - cluster-allow-replica-migration
    - cluster-announce-bus-port
    - cluster-announce-hostname
    - cluster-announce-human-nodename
    - cluster-announce-ip
    - cluster-announce-port
    - cluster-announce-tls-port
    - cluster-compatibility-sample-ratio
    - cluster-config-file
    - cluster-enabled
    - cluster-link-sendbuf-limit
    - cluster-migration-barrier
    - cluster-node-timeout
    - cluster-ping-interval
    - cluster-port
    - cluster-preferred-endpoint-type
    - cluster-replica-no-failover
    - cluster-replica-validity-factor
    - cluster-require-full-coverage
    - cluster-slave-no-failover
    - cluster-slave-validity-factor
    - crash-log-enabled
    - crash-memcheck-enabled
    - daemonize
    - databases
    - dbfilename
    - dir
    - disable-thp
    - dynamic-hz
    - enable-debug-command
    - enable-module-command
    - enable-protected-configs
    - hash-max-listpack-entries
    - hash-max-listpack-value
    - hash-max-ziplist-entries
    - hash-max-ziplist-value
    - hide-user-data-from-log
    - hll-sparse-max-bytes
    - hz
    - ignore-warnings
    - include
    - io-threads
    - jemalloc-bg-thread
    - key-load-delay
    - latency-monitor-threshold
    - latency-tracking
    - latency-tracking-info-percentiles
    - lazyfree-lazy-eviction
    - lazyfree-lazy-expire
    - lazyfree-lazy-server-del
    - lazyfree-lazy-user-del
    - lazyfree-lazy-user-flush
    - lfu-decay-time
    - lfu-log-factor
    - list-compress-depth
    - list-max-listpack-size
    - list-max-ziplist-size
    - loading-process-events-interval-bytes
    - loadmodule
    - locale-collate
    - logfile
    - loglevel
    - lua-enable-deprecated-api
    - lua-time-limit
    - masterauth
    - masteruser
    - max-new-connections-per-cycle
    - max-new-tls-connections-per-cycle
    - maxclients
    - maxmemory
    - maxmemory-clients
    - maxmemory-eviction-tenacity
    - maxmemory-policy
    - maxmemory-samples
    - min-replicas-max-lag
    - min-replicas-to-write
    - min-slaves-max-lag
    - min-slaves-to-write
    - no-appendfsync-on-rewrite
    - notify-keyspace-events
    - oom-score-adj
    - oom-score-adj-values
    - pidfile
    - port
    - proc-title-template
    - propagation-error-behavior
    - protected-mode
    - proto-max-bulk-len
    - rdb-del-sync-files
    - rdb-key-save-delay
    - rdb-save-incremental-fsync
    - rdbchecksum
    - rdbcompression
    - rename-command
    - repl-backlog-size
    - repl-backlog-ttl
    - repl-disable-tcp-nodelay
    - repl-diskless-load
    - repl-diskless-sync
    - repl-diskless-sync-delay
    - repl-diskless-sync-max-replicas
    - repl-ping-replica-period
    - repl-ping-slave-period
    - repl-rdb-channel
    - repl-timeout
    - replica-announce-ip
    - replica-announce-port
    - replica-announced
    - replica-full-sync-buffer-limit
    - replica-ignore-disk-write-errors
    - replica-ignore-maxmemory
    - replica-lazy-flush
    - replica-priority
    - replica-read-only
    - replica-serve-stale-data
    - replicaof
    - req-res-logfile
    - requirepass
    - sanitize-dump-payload
    - save
    - sentinel
    - server-cpulist
    - server_cpulist
    - set-max-intset-entries
    - set-max-listpack-entries
    - set-max-listpack-value
    - set-proc-title
    - shutdown-on-sigint
    - shutdown-on-sigterm
    - shutdown-timeout
    - slave-announce-ip
    - slave-announce-port
    - slave-ignore-maxmemory
    - slave-lazy-flush
    - slave-priority
    - slave-read-only
    - slave-serve-stale-data
    - slaveof
    - slowlog-log-slower-than
    - slowlog-max-len
    - socket-mark-id
    - stop-writes-on-bgsave-error
    - stream-node-max-bytes
    - stream-node-max-entries
    - supervised
    - syslog-enabled
    - syslog-facility
    - syslog-ident
    - tcp-backlog
    - tcp-keepalive
    - timeout
    - tls-auth-clients
    - tls-ca-cert-dir
    - tls-ca-cert-file
    - tls-cert-file
    - tls-ciphers
    - tls-ciphersuites
    - tls-client-cert-file
    - tls-client-key-file
    - tls-client-key-file-pass
    - tls-cluster
    - tls-dh-params-file
    - tls-key-file
    - tls-key-file-pass
    - tls-port
    - tls-prefer-server-ciphers
    - tls-protocols
    - tls-replication
    - tls-session-cache-size
    - tls-session-cache-timeout
    - tls-session-caching
    - tracking-table-max-keys
    - unixsocket
    - unixsocketperm
    - use-exit-on-panic
    - user
    - watchdog-period
    - zset-max-listpack-entries
    - zset-max-listpack-value
    - zset-max-ziplist-entries
    - zset-max-ziplist-value
  "8.2":
    - acl-pubsub-default
    - aclfile
    - acllog-max-len
    - active-defrag-cycle-max
    - active-defrag-cycle-min
    - active-defrag-ignore-bytes
    - active-defrag-max-scan-fields
    - active-defrag-threshold-lower
    - active-defrag-threshold-upper
    - active-expire-effort
    - activedefrag
    - activerehashing
    - always-show-logo
    - aof-disable-auto-gc
    - aof-load-truncated
    - aof-rewrite-cpulist
    - aof-rewrite-incremental-fsync
    - aof-timestamp-enabled
    - aof-use-rdb-preamble
    - aof_rewrite_cpulist
    - appenddirname
    - appendfilename
    - appendfsync
    - appendonly
    - auto-aof-rewrite-min-size
    - auto-aof-rewrite-percentage
    - bgsave-cpulist
    - bgsave_cpulist
    - bind
    - bind-source-addr
    - bio-cpulist
    - bio_cpulist
    - busy-reply-threshold
    - client-default-resp
    - client-output-buffer-limit
    - client-query-buffer-limit
    - cluster-allow-pubsubshard-when-down
    - cluster-allow-reads-when-down
    - cluster-allow-replica-migration
    - cluster-announce-bus-port
    - cluster-announce-hostname
    - cluster-announce-human-nodename
    - cluster-announce-ip
    - cluster-announce-port
    - cluster-announce-tls-port
    - cluster-compatibility-sample-ratio
    - cluster-config-file
    - cluster-enabled
    - cluster-link-sendbuf-limit
    - cluster-migration-barrier
    - cluster-node-timeout
    - cluster-ping-interval
    - cluster-port
    - cluster-preferred-endpoint-type
    - cluster-replica-no-failover
    - cluster-replica-validity-factor
    - cluster-require-full-coverage
    - cluster-slave-no-failover
    - cluster-slave-validity-factor
    - cluster-slot-stats-enabled
    - crash-log-enabled
    - crash-memcheck-enabled
    - daemonize
    - databases
    - dbfilename
    - dir
    - disable-thp
    - dynamic-hz
    - enable-debug-command
    - enable-module-command
    - enable-protected-configs
    - hash-max-listpack-entries
    - hash-max-listpack-value
    - hash-max-ziplist-entries
    - hash-max-ziplist-value
    - hide-user-data-from-log
    - hll-sparse-max-bytes
    - hz
    - ignore-warnings
    - include
    - io-threads
    - jemalloc-bg-thread
    - key-load-delay
    - latency-monitor-threshold
    - latency-tracking
    - latency-tracking-info-percentiles
    - lazyexpire-nested-arbitrary-keys
    - lazyfree-lazy-eviction
    - lazyfree-lazy-expire
    - lazyfree-lazy-server-del
    - lazyfree-lazy-user-del
    - lazyfree-lazy-user-flush
    - lfu-decay-time
    - lfu-log-factor
    - list-compress-depth
    - list-max-listpack-size
    - list-max-ziplist-size
    - loading-process-events-interval-bytes
    - loadmodule
    - locale-collate
    - logfile
    - loglevel
    - lua-enable-deprecated-api
    - lua-time-limit
    - masterauth
    - masteruser
    - max-new-connections-per-cycle
    - max-new-tls-connections-per-cycle
    - maxclients
    - maxmemory
    - maxmemory-clients
    - maxmemory-eviction-tenacity
    - maxmemory-policy
    - maxmemory-samples
    - min-replicas-max-lag
    - min-replicas-to-write
    - min-slaves-max-lag
    - min-slaves-to-write
    - no-appendfsync-on-rewrite
    - notify-keyspace-events
    - oom-score-adj
    - oom-score-adj-values
    - pidfile
    - port
    - prefetch-batch-max-size
    - proc-title-template
    - propagation-error-behavior
    - protected-mode
    - proto-max-bulk-len
    - rdb-del-sync-files
    - rdb-key-save-delay
    - rdb-save-incremental-fsync
    - rdbchecksum
    - rdbcompression
    - rename-command
    - repl-backlog-size
    - repl-backlog-ttl
    - repl-disable-tcp-nodelay
    - repl-diskless-load
    - repl-diskless-sync
    - repl-diskless-sync-delay
    - repl-diskless-sync-max-replicas
    - repl-ping-replica-period
    - repl-ping-slave-period
    - repl-rdb-channel
    - repl-timeout
    - replica-announce-ip
    - replica-announce-port
    - replica-announced
    - replica-full-sync-buffer-limit
    - replica-ignore-disk-write-errors
    - replica-ignore-maxmemory
    - replica-lazy-flush
    - replica-priority
    - replica-read-only
    - replica-serve-stale-data
    - replicaof
    - req-res-logfile
    - requirepass
    - sanitize-dump-payload
    - save
    - sentinel
    - server-cpulist
    - server_cpulist
    - set-max-intset-entries
    - set-max-listpack-entries
    - set-max-listpack-value
    - set-proc-title
    - shutdown-on-sigint
    - shutdown-on-sigterm
    - shutdown-timeout
    - slave-announce-ip
    - slave-announce-port
    - slave-ignore-maxmemory
    - slave-lazy-flush
    - slave-priority
    - slave-read-only
    - slave-serve-stale-data
    - slaveof
    - slowlog-log-slower-than
    - slowlog-max-len
    - socket-mark-id
    - stop-writes-on-bgsave-error
    - stream-node-max-bytes
    - stream-node-max-entries
    - supervised
    - syslog-enabled
    - syslog-facility
    - syslog-ident
    - tcp-backlog
    - tcp-keepalive
    - timeout
    - tls-auth-clients
    - tls-ca-cert-dir
    - tls-ca-cert-file
    - tls-cert-file
    - tls-ciphers
    - tls-ciphersuites
    - tls-client-cert-file
    - tls-client-key-file
    - tls-client-key-file-pass
    - tls-cluster
    - tls-dh-params-file
    - tls-key-file
    - tls-key-file-pass
    - tls-port
    - tls-prefer-server-ciphers
    - tls-protocols
    - tls-replication
    - tls-session-cache-size
    - tls-session-cache-timeout
    - tls-session-caching
    - tracking-table-max-keys
    - unixsocket
    - unixsocketperm
    - use-exit-on-panic
    - user
    - watchdog-period
    - zset-max-listpack-entries
    - zset-max-listpack-value
    - zset-max-ziplist-entries
    - zset-max-ziplist-value
