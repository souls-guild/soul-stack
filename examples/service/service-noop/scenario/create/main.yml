name: create
description: No-op bootstrap — smoke-вызов core.exec.run для E2E scenario-runner-а.

# Сценарий ничего не пишет в incarnation.state: state_schema пустой.
state_changes: {}

tasks:
  - name: Echo hello on every host of the incarnation
    module: core.exec.run
    params:
      cmd: echo
      args: ["hello"]
    changed_when: "false"
