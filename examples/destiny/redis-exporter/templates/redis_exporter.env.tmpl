{{/*
  Root-only EnvironmentFile. quote emits a systemd-compatible
  double-quoted value, so spaces/quotes/backslashes cannot create a second assignment.
*/}}
REDIS_PASSWORD={{ .vars.password | quote }}
