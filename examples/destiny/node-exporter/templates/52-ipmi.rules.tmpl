{{/* udev-правило: даёт группе .vars.user (node_exporter) доступ к /dev/ipmi*.
     Движок — Go text/template strict (ADR-010); единственная переменная — user. */ -}}
KERNEL=="ipmi*", MODE="664", GROUP="{{ .vars.user }}"
