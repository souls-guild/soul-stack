{{/* udev rule: grants the .vars.user group (node_exporter) access to /dev/ipmi*.
     Engine is Go text/template strict (ADR-010); the only variable is user. */ -}}
KERNEL=="ipmi*", MODE="664", GROUP="{{ .vars.user }}"
