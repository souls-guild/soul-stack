name: redis
description: >-
  Режим-агностичный per-host кирпич Redis (концепция Ansible-роли, B-гибрид
  ADR-009): установка пакета + render redis.conf из merged-конфига + render
  users.acl + systemd hardening drop-in + сервис. ВСЯ оркестрация (топология,
  rolling-restart, failover, sentinel reconcile, health-gate) живёт в scenario
  сервиса — здесь только идемпотентный install/render/service одного инстанса.

# Версия destiny — git ref (ADR-007). Поля `version:` верхнего уровня нет.
# Список задач — в tasks/main.yml. Только core-модули → required_modules: не нужен
# (community.redis вызывается из scenario сервиса, не из этой destiny).
#
# Режим-агностична: один и тот же кирпич обслуживает standalone, cluster и
# sentinel (последний — через sentinel_enabled + sentinel.conf.tmpl/unit, gated-
# задачи в хвосте tasks/main.yml). Режим выбирает scenario сервиса передачей флагов
# и cluster-/sentinel-директив в config; destiny сама режим не знает. Конфиг
# приходит ГОТОВЫМ merged map-ом через apply: input: config: (merge default→preset→
# вычисления→passthrough делает scenario сервиса) — destiny остаётся «глупой», сама
# ничего не сливает.

input:
  # ── Гейт data-плоскости redis-server (режим sentinel_only) ───────────────────
  # deploy_redis=false → НЕ разворачиваем redis-server (data-плоскость): redis.conf,
  # users.acl, PEM/TLS data-плоскости, systemd hardening drop-in и core.service
  # redis-server running/restart — все gated placeholder-skip-ом. Пакет redis при
  # этом ставится ВСЕГДА (sentinel-демон поставляется ТЕМ ЖЕ пакетом). Так режим
  # sentinel_only поднимает ТОЛЬКО sentinel-демон (sentinel_enabled=true,
  # deploy_redis=false), мониторящий ВНЕШНИЙ master (адрес из input.sentinel.*).
  # default:true — стандартный путь (standalone/cluster/sentinel разворачивают
  # redis-server). Гейт host-инвариантен → приходит через apply.input. Предикат
  # static (только input.*) → Keeper гасит data-задачи placeholder-skip-ом без
  # сдвига индексов (skip занимает слот).
  deploy_redis:
    type: boolean
    required: false
    default: true
    description: >-
      Разворачивать ли data-плоскость redis-server (render redis.conf/users.acl/
      PEM + systemd-сервис redis-server). По умолчанию true. false (режим
      sentinel_only) — поднять только sentinel-демон (sentinel_enabled), master
      внешний. Пакет redis ставится в любом случае (несёт и sentinel-демон).

  version:
    type: string
    required: true
    # distro-native: опциональный epoch `N:` + upstream-версия + опциональная
    # ревизия (напр. Debian `5:7.0.15-1~deb12u7`). Узкий semver ломал version-пин
    # на реальном дистрибутиве (apt-get install redis-server=7.2.4 → exit 100).
    pattern: "^([0-9]+:)?[0-9][A-Za-z0-9.~+-]*$"
    description: >-
      Версия пакета redis-server в репозитории дистрибутива (distro-native пин,
      напр. 5:7.0.15-1~deb12u7). ОБЯЗАТЕЛЬНА (директива пользователя 2026-06-23):
      core.pkg всегда ставит =version-пин. Поведение «latest из репо» (пустой
      version → core.pkg без пина) более НЕ доступно — пин фиксирует воспроизводимую
      инсталляцию.

  password:
    type: string
    required: true
    secret: true
    min_length: 16
    description: >-
      Пароль Redis (requirepass). Маскируется в логах/трейсах/UI. Destiny
      получает УЖЕ зарезолвленное значение: caller-сценарий читает секрет из Vault
      keeper-side через CEL-функцию vault() в render-фазе (templating.md §2.3/§4)
      и прокидывает результат в apply:input. Destiny остаётся «глупой» — кладёт
      готовый пароль в redis.conf, vault-клиент не тянет (ADR-012).

  # Merged-конфиг redis.conf: caller (scenario сервиса) сливает default→preset→
  # вычисления→passthrough через merge() и передаёт ГОТОВЫЙ map. Шаблон range-ит
  # его ПОСЛЕ базовых директив (last-wins): caller может осознанно переопределить
  # любую базовую. Пустой/не передан → секция пуста. unixsocket/maxmemory/save/
  # appendonly/maxmemory-policy — обычные ключи этого map-а (scenario их вычислил).
  config:
    type: object
    description: >-
      Готовые директивы redis.conf (merged на стороне scenario сервиса). Каждая
      пара key→value пишется в redis.conf как есть. Destiny НЕ мержит повторно.
    properties:
      maxclients:
        type: integer
        min: 1
      timeout:
        type: integer
        min: 0
    additional_properties: true

  # ACL-пользователи Redis: MAP имя→{perms, state, password}. scenario строит
  # этот map из своего typed-map input.users (формой merge(list(map)) над
  # .map(...)-comprehension) и резолвит password каждого юзера keeper-side из
  # Vault. Шаблон users.acl.tmpl range-ит MAP (Go сортирует ключи → строки
  # ACL детерминированы между прогонами — нет ложного change/рестарта) и хеширует
  # пароль (Redis ACL #<sha256>) — plaintext в файл НЕ пишется. Пустой/не передан
  # → users.acl без юзеров.
  users:
    type: object
    required: false
    description: >-
      ACL-пользователи Redis (map имя→{perms, state, password}). Рендерятся в
      /etc/redis/users.acl (aclfile в redis.conf), порядок строк детерминирован
      (range по map сортирует ключи). password — зарезолвленный keeper-side
      секрет (маскируется); в файл пишется его sha256-хеш.
    properties: {}
    additional_properties:
      type: object
      required: [perms, state, password]
      additional_properties: false
      properties:
        perms:
          type: string
        state:
          type: string
          enum: [on, off]
        password:
          type: string
          secret: true

  # ── TLS (концепция Ansible-роли redis: redis_tls_*) ──────────────────────────
  # PEM-материал и директивы TLS — HOST-ИНВАРИАНТ (один cert/CA/порт на кластер) →
  # приходят через apply.input (scenario резолвит PEM из Vault в render-фазе). Гейт
  # tls_enable на трёх PEM-render-задачах (в хвосте tasks/main.yml). Сами TLS-
  # директивы (tls-port/tls-cert-file/...) — обычные ключи merged config (scenario
  # кладёт их в config); destiny их range-ит в redis.conf как любые директивы.
  # tls_only НЕ читается destiny как отдельная задача — он лишь меняет config
  # (scenario ставит `port 0` при tls_only), здесь объявлен для контракта/доки.
  tls_enable:
    type: boolean
    required: false
    default: false
    description: >-
      Включить TLS: рендерить PEM-файлы /etc/redis/tls/{redis.crt,redis.key,ca.crt}
      (mode 0600 owner redis) и (через config) TLS-директивы redis.conf. По
      умолчанию false (plaintext).

  tls_only:
    type: boolean
    required: false
    default: false
    description: >-
      Только-TLS: plain-порт закрыт (`port 0` в config, ставит scenario). Плагин
      community.redis ОБЯЗАН коннектиться по TLS. Объявлено для контракта; destiny
      его прямо не читает (эффект — в merged config от scenario).

  tls_port:
    type: integer
    required: false
    default: 7379
    min: 1
    description: >-
      TLS-порт Redis (директива tls-port redis.conf, приходит в config от
      scenario). По умолчанию 7379 (как redis_tls_port роли).

  tls_cert:
    type: string
    required: false
    secret: true
    description: >-
      PEM серверного сертификата (рендерится в /etc/redis/tls/redis.crt).
      Зарезолвлен keeper-side из Vault (render-фаза vault()); маскируется.

  tls_key:
    type: string
    required: false
    secret: true
    description: >-
      PEM приватного ключа серверного сертификата (рендерится в
      /etc/redis/tls/redis.key, mode 0600). Секрет; маскируется, в файл — как есть.

  tls_ca:
    type: string
    required: false
    secret: true
    description: >-
      PEM CA-сертификата кластера (рендерится в /etc/redis/tls/ca.crt). Узлы
      валидируют сертификаты пиров по нему (tls-replication/tls-cluster).

  # ── Redis-модули (концепция Ansible-роли redis: redis_module_*) ──────────────
  # На Redis < 8 модули (RediSearch / RedisJSON / RedisTimeSeries / RedisBloom)
  # подключаются как загружаемые .so-объекты: их .so качаются в modules_dir и
  # подключаются директивой loadmodule в redis.conf. На Redis 8+ модули ВСТРОЕНЫ в
  # сервер — .so не качаем и loadmodule НЕ пишем (version-gate делает scenario: на
  # 8+ передаёт modules=[] и loadmodules=[]). HOST-ИНВАРИАНТ (один набор модулей на
  # кластер) → приходят через apply.input, не per-host .self (tiling-урок).
  #
  # modules — короткие алиасы. Имя .so-файла каждого алиаса — фиксированная таблица
  # имён Redis-модулей (НЕ operator-трансляция) → живёт в destiny (тут). URL .so
  # АРХ-СПЕЦИФИЧЕН: destiny строит его per-host из modules_base_url + СВОЕГО
  # soulprint.self.os.arch + имени файла (soulprint доступен в destiny-CEL —
  # прецедент arch-резолва node-exporter), поэтому mixed-arch incarnation корректен
  # (каждый хост качает .so под свою арх). checksum (modules_sha256) — host-инвариант
  # (для архитектуры кластера) → apply.input, как single sha256 redis-exporter.
  modules:
    type: array
    required: false
    description: >-
      Алиасы Redis-модулей для загрузки (Redis < 8). Пусто/не передан → модули не
      качаются и не подключаются. На Redis 8+ scenario передаёт пустой список
      (модули встроены в сервер).
    items:
      type: string
      enum: [search, json, timeseries, bloom]

  modules_dir:
    type: string
    required: false
    default: /var/lib/redis/modules
    pattern: "^/[A-Za-z0-9._/-]+$"
    description: >-
      Каталог, куда раскладываются .so Redis-модулей (концепция redis_module_dir
      роли). По умолчанию /var/lib/redis/modules.

  modules_base_url:
    type: string
    required: false
    description: >-
      Базовый URL источника .so Redis-модулей (без арх-сегмента). destiny строит
      полный URL каждого модуля как modules_base_url/<arch>/<имя.so>, где arch —
      soulprint.self.os.arch ЭТОГО хоста. Обязателен, если modules непуст.

  # checksum каждого .so — ОБЯЗАТЕЛЬНЫЙ, fail-closed (прецедент redis-exporter):
  # core.url.fetched верифицирует SHA-256 ДО публикации файла, mismatch → задача
  # падает (целевой путь нетронут). Без placeholder-дефолта: нет хэша → честный
  # schema-отказ, а не fetch с заведомо отвергаемым плейсхолдером. Форма значения —
  # голый hex (destiny оборачивает в "sha256:<hex>" под контракт core.url.fetched).
  modules_sha256:
    type: object
    required: false
    description: >-
      Маппинг алиас→SHA-256 .so-объекта (голый hex, обязателен для каждого
      загружаемого модуля). Соответствует паре (version, arch) кластера; берётся из
      sha256sums источника модулей. Host-инвариант (одна арх на кластер в типичном
      случае) → приходит через apply.input.
    properties: {}
    additional_properties:
      type: string
      pattern: "^[0-9a-f]{64}$"

  # ── Host-tuning extras (концепция Ansible-роли) ──────────────────────────────
  # Режим-агностичные per-host добавки. THP-отключение / logrotate / sysctl —
  # БЕЗУСЛОВНЫ (рекомендация Redis / hardening, не выбор оператора): флагов в
  # контракте нет, задачи всегда активны. sysctl_settings — данные-таблица значений
  # kernel-параметров, host-инвариантна → приходит через apply.input (scenario
  # сервиса), сами параметры применяются всегда.
  sysctl_settings:
    type: object
    required: false
    description: >-
      Kernel-параметры под Redis (map имя→значение), рендерятся в
      /etc/sysctl.d/30-redis.conf. Значения — СТРОКИ (multi-value параметры через
      пробел). Передаются scenario сервиса (дефолты — essence.sysctl_settings).
    properties: {}
    additional_properties: true

  # ── Sentinel (режим sentinel) ────────────────────────────────────────────────
  # Эти поля задействованы ТОЛЬКО когда sentinel_enabled=true (gated-задачи ниже
  # в tasks/main.yml). Вне sentinel-режима не передаются → has()-guard в задачах
  # гасит рендер sentinel.conf/unit. Структура — режим-агностичный кирпич остаётся
  # «глупым»: scenario сервиса вычисляет master-election и передаёт ГОТОВЫЕ значения.
  sentinel_enabled:
    type: boolean
    required: false
    default: false
    description: >-
      Включить рендер sentinel.conf + redis-sentinel.service + запуск демона
      (режим sentinel). По умолчанию false (standalone/cluster — без sentinel).

  sentinel:
    type: object
    # Поле опционально (field-level required отсутствует → false): передаётся
    # только в sentinel-режиме (gated на sentinel_enabled). Когда передано —
    # master_ip обязателен (object-level required: [...] ниже). Один ключ
    # `required` может нести ЛИБО bool (опциональность поля), ЛИБО список
    # обязательных под-ключей object-а — здесь выбрана списочная форма, поэтому
    # `required: false` отсутствует (опциональность достигается дефолтом).
    description: >-
      Параметры monitored master для sentinel.conf. master_ip/master_port —
      HOST-ИНВАРИАНТНЫЙ адрес master-а кластера (один на кластер, scenario
      резолвит run_once). announce-ip НЕ здесь: он per-host, рендерится из
      .self.network.primary_ip. config — стартовые директивы sentinel.conf
      (файловая форма). auth_pass — зарезолвленный keeper-side секрет (маскируется).
    # master_ip обязателен: sentinel без monitored master бессмыслен, а задача
    # рендера sentinel.conf берёт ${ input.sentinel.master_ip } БЕЗ has()-guard
    # (gated на sentinel_enabled). Required ловит пропуск адреса понятной
    # schema-ошибкой ДО render, а не криптичным CEL «no such key» в шаблоне.
    required: [master_ip]
    additional_properties: false
    properties:
      master_name:
        type: string
      master_ip:
        type: string
      master_port:
        type: integer
        min: 1
      quorum:
        type: integer
        min: 1
      auth_user:
        type: string
      auth_pass:
        type: string
        secret: true
      config:
        type: object
        properties: {}
        additional_properties: true
