# EnvironmentFile для keeper.service. Путь к конфигу keeper-демона.
# Дефолтный путь бинаря — /etc/keeper/keeper.yml; здесь его можно переопределить
# без правки самого юнита (например, на /etc/keeper/keeper.prod.yml).
KEEPER_CONFIG=/etc/keeper/keeper.yml
