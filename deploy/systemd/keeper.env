# EnvironmentFile for keeper.service. Path to the keeper daemon's config.
# The binary's default path is /etc/keeper/keeper.yml; it can be overridden here
# without editing the unit itself (for example, to /etc/keeper/keeper.prod.yml).
KEEPER_CONFIG=/etc/keeper/keeper.yml
